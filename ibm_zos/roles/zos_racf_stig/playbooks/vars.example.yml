---
# Example overrides for zos_racf_stig (-e @vars.example.yml).

# Default: generate the checklist only (no mainframe contacted).
zos_live_assessment: false

# To run the live read-only assessment, set both:
# zos_live_assessment: true
# zos_target: zos-lpar-01     # must exist in the [zos] inventory group
