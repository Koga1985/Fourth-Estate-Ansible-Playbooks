---
# Example overrides for dynatrace_appsec (-e @vars.example.yml).
apply_changes: false
dt_environment_url: "https://abc12345.live.dynatrace.com"
dt_api_token: "{{ vault_dt_api_token }}"

# Data-driven operations (apply_changes=true only) - validate against your
# tenant's Settings 2.0 schema (GET /api/v2/settings/schemas/builtin:appsec.attack-protection-advanced-config).
dt_appsec_operations: []
