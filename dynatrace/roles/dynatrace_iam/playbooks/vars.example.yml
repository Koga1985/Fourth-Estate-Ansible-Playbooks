---
# Example overrides for dynatrace_iam (-e @vars.example.yml).
apply_changes: false
dt_account_uuid: "{{ vault_dt_account_uuid }}"
dt_oauth_client_id: "{{ vault_dt_oauth_client_id }}"
dt_oauth_client_secret: "{{ vault_dt_oauth_client_secret }}"

# Data-driven IAM operations (apply_changes=true only) - validate against the
# Account Management API docs first.
dt_iam_operations: []
#
# Example - create a group:
# dt_iam_operations:
#   - id: "GRP-READONLY"
#     title: "Create read-only group"
#     method: POST
#     path: "accounts/{{ dt_account_uuid }}/groups"
#     status_code: [201, 200]
#     body:
#       - name: "FourthEstate-ReadOnly"
#         description: "Read-only auditors"
