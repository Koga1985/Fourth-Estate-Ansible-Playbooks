---
# Example overrides for dynatrace_config_backup (-e @vars.example.yml).
dt_environment_url: "https://abc12345.live.dynatrace.com"
dt_api_token: "{{ vault_dt_api_token }}"
backup_base_dir: "/var/backups/dynatrace"
