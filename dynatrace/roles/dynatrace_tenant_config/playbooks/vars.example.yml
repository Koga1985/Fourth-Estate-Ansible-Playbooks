---
# Example overrides for dynatrace_tenant_config (-e @vars.example.yml).
apply_changes: false
dt_environment_url: "https://abc12345.live.dynatrace.com"
dt_api_token: "{{ vault_dt_api_token }}"

# Data-driven config operations (applied only with apply_changes=true).
# Validate against your tenant's API docs (Settings 2.0 / Config API).
dt_config_operations: []
#
# Example - create a management zone and an alerting profile:
# dt_config_operations:
#   - id: "MZ-PROD"
#     title: "Management zone: Production"
#     method: POST
#     path: "config/v1/managementZones"
#     status_code: [201, 200]
#     body:
#       name: "Production"
#       rules: []
#   - id: "AP-CRITICAL"
#     title: "Alerting profile: Critical"
#     method: POST
#     path: "config/v1/alertingProfiles"
#     status_code: [201, 200]
#     body:
#       displayName: "Critical-Only"
#       rules:
#         - severityLevel: "AVAILABILITY"
#           delayInMinutes: 0
#           tagFilterIncludeMode: "NONE"
