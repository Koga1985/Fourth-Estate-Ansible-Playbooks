---
# Example overrides for dynatrace_data_privacy (-e @vars.example.yml).
apply_changes: false
dt_environment_url: "https://abc12345.live.dynatrace.com"
dt_api_token: "{{ vault_dt_api_token }}"

# Data-driven operations (apply_changes=true only) - validate against your
# tenant's Settings 2.0 schema (GET /api/v2/settings/schemas/builtin:preferences.privacy).
dt_privacy_operations: []
