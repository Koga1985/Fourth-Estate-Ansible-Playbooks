---
# Dynatrace Ansible collection requirements.
collections:
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.windows
    version: ">=1.14.0"
  - name: kubernetes.core      # dynatrace_kubernetes (DynaKube CR / Secret on K8s/OpenShift)
    version: ">=3.0.0"
