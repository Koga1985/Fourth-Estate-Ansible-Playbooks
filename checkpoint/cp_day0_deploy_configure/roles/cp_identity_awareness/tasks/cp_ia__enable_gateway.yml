
- name: Enable IA on gateways
  check_point.mgmt.cp_mgmt_simple_gateway:
    name: "{{ item.name }}"
    identity_aware: "{{ item.identity_aware | default(true) }}"
    state: present
  loop: "{{ ia_gateways | default([]) }}"
