---
# Check Point Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: check_point.mgmt
    version: ">=5.0.0"

  - name: check_point.gaia
    version: ">=5.0.0"

  - name: community.general
    version: ">=8.0.0"

# Python dependencies:
# pip install cpapi
