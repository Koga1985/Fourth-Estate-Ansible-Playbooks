---
# Role defaults
publish_changes: false

# Expected input (examples)
# L4 services (TCP/UDP)
cp_services_tcp: []   # - { name, port, session_timeout?, match_for_any?, color?, comment? }
cp_services_udp: []   # - { name, port, session_timeout?, match_for_any?, color?, comment? }

# L7 services (Application objects / URL categories)
cp_app_sites: []      # - { name, primary_category?, url_list?, additional_categories?, risk?, color?, comment? }
cp_app_categories: [] # - { name, color?, comment? }
