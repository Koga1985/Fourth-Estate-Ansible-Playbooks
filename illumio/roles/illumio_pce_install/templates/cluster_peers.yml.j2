# REVIEW REQUIRED before production - PCE cluster topology. See KNOWN_LIMITATIONS.md (#13).
nodes:
{% for peer in illumio_pce_cluster_peers | default([]) %}
  - hostname: {{ peer.hostname }}
    roles: {{ peer.roles | default(['core']) | to_json }}
{% endfor %}
