---
# Illumio PCE load balancer integration — NOT YET IMPLEMENTED (documented gap)
#
# This workflow is intentionally a fail-fast placeholder rather than unverified
# automation against front-end load balancers, which would be unsafe to ship untested.
# See KNOWN_LIMITATIONS.md and PRODUCTION_READINESS_ASSESSMENT.md.
#
# The default path is unaffected: this only runs when illumio_pce_use_loadbalancer is true.
- name: Illumio PCE load balancer integration is not implemented
  ansible.builtin.fail:
    msg: >-
      The 'load_balancer' workflow is not implemented in this role yet.
      Provide a site-specific implementation, or do not enable it (illumio_pce_use_loadbalancer is true),
      before running this path in production.
