---
# Illumio PCE database cluster setup — NOT YET IMPLEMENTED (documented gap)
#
# This workflow is intentionally a fail-fast placeholder rather than unverified
# automation against a production PCE database cluster, which would be unsafe to ship untested.
# See KNOWN_LIMITATIONS.md and PRODUCTION_READINESS_ASSESSMENT.md.
#
# The default path is unaffected: this only runs when illumio_pce_cluster_mode is true.
- name: Illumio PCE database cluster setup is not implemented
  ansible.builtin.fail:
    msg: >-
      The 'database_cluster' workflow is not implemented in this role yet.
      Provide a site-specific implementation, or do not enable it (illumio_pce_cluster_mode is true),
      before running this path in production.
