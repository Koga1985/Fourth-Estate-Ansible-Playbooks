---
galaxy_info:
  role_name: gcp_cloudrun_locked_down
  description: Cloud Run defaults: internal-only, CMEK, min TLS, CPU/mem limits; domain restrictions; per-service exceptions.
  license: MIT
  min_ansible_version: "2.12"
dependencies: []
