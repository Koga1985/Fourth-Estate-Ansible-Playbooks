---
# Google Cloud Platform Enterprise Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: google.cloud
    version: ">=1.2.0"

  - name: community.general
    version: ">=8.0.0"

  - name: kubernetes.core
    version: ">=2.4.0"

  - name: ansible.posix
    version: ">=1.5.0"

# Python dependencies:
# pip install google-auth google-api-python-client google-cloud-storage
# pip install google-cloud-bigquery google-cloud-pubsub google-cloud-logging
