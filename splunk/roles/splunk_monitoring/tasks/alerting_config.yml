---
# Alerting configuration

- name: Configure email alerting
  ansible.builtin.lineinfile:
    path: "{{ splunk_home }}/etc/system/local/alert_actions.conf"
    line: "to = {{ monitoring_alert_email }}"
    insertafter: '^\[email\]'
    create: yes
  when: monitoring_alert_email != ''
