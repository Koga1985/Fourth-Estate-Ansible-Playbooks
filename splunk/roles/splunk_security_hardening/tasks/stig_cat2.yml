---
# STIG Category II (Medium Severity) Findings Remediation

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['splunk/splunk_security_hardening/stig_cat2']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_splunk_splunk_security_hardening_stig_cat2_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "splunk/splunk_security_hardening/stig_cat2 | Initialize validation state"
  ansible.builtin.set_fact:
    fe_splunk_splunk_security_hardening_stig_cat2_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_splunk_splunk_security_hardening_stig_cat2_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_splunk_splunk_security_hardening_stig_cat2_status: "running"
    fe_splunk_splunk_security_hardening_stig_cat2_preflight_passed: false
    fe_splunk_splunk_security_hardening_stig_cat2_postflight_passed: false
    fe_splunk_splunk_security_hardening_stig_cat2_failed_task: ""
    fe_splunk_splunk_security_hardening_stig_cat2_error: ""
    fe_splunk_splunk_security_hardening_stig_cat2_stats_published: false
  tags:
    - always

- name: "splunk/splunk_security_hardening/stig_cat2 | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "splunk/splunk_security_hardening/stig_cat2 | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          splunk/splunk_security_hardening/stig_cat2: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          splunk/splunk_security_hardening/stig_cat2: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "splunk/splunk_security_hardening/stig_cat2 | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "splunk/splunk_security_hardening/stig_cat2: required variable '{{ item }}' is not defined."
        success_msg: "splunk/splunk_security_hardening/stig_cat2: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_splunk_splunk_security_hardening_stig_cat2_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "splunk/splunk_security_hardening/stig_cat2 | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_splunk_splunk_security_hardening_stig_cat2_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: V-258120 - Configure password complexity
      ansible.builtin.blockinfile:
        path: "{{ splunk_home }}/etc/system/local/authentication.conf"
        block: |
          [authentication]
          minPasswordLength = {{ ia_password_min_length }}
          minPasswordDigit = 1
          minPasswordLowercase = 1
          minPasswordUppercase = 1
          minPasswordSpecial = 1
        marker: "# {mark} CAT2 V-258120 ANSIBLE MANAGED BLOCK"
      notify: restart splunk
      tags:
        - CAT2
        - V-258120

    - name: V-258122 - Configure account lockout
      ansible.builtin.blockinfile:
        path: "{{ splunk_home }}/etc/system/local/authentication.conf"
        block: |
          lockoutAttempts = {{ ac_max_failed_login_attempts }}
          lockoutThresholdMins = {{ ac_account_lockout_duration_minutes }}
          lockoutMins = {{ ac_account_lockout_duration_minutes }}
        marker: "# {mark} CAT2 V-258122 ANSIBLE MANAGED BLOCK"
      notify: restart splunk
      tags:
        - CAT2
        - V-258122

    - name: V-258124 - Enable password history
      ansible.builtin.lineinfile:
        path: "{{ splunk_home }}/etc/system/local/authentication.conf"
        line: "passwordHistoryCount = {{ ia_password_history_count }}"
      notify: restart splunk
      tags:
        - CAT2
        - V-258124

    - name: V-258126 - Disable unnecessary services
      ansible.builtin.lineinfile:
        path: "{{ splunk_home }}/etc/system/local/web.conf"
        line: "enableWebDebug = false"
      notify: restart splunk
      tags:
        - CAT2
        - V-258126

    - name: V-258128 - Enable secure cookie flag
      ansible.builtin.lineinfile:
        path: "{{ splunk_home }}/etc/system/local/web.conf"
        line: "cookieSecure = true"
      notify: restart splunk
      tags:
        - CAT2
        - V-258128

    # ----------------------------- postflight -------------------

    - name: "splunk/splunk_security_hardening/stig_cat2 | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_splunk_splunk_security_hardening_stig_cat2_preflight_passed | bool
          - fe_splunk_splunk_security_hardening_stig_cat2_status in ['running', 'succeeded']
        fail_msg: >-
          splunk/splunk_security_hardening/stig_cat2: postflight validation failed - preflight passed
          ={{ fe_splunk_splunk_security_hardening_stig_cat2_preflight_passed | default(false) }},
          status={{ fe_splunk_splunk_security_hardening_stig_cat2_status | default('unknown') }}.
        success_msg: "splunk/splunk_security_hardening/stig_cat2: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "splunk/splunk_security_hardening/stig_cat2 | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_splunk_splunk_security_hardening_stig_cat2_postflight_passed: true
        fe_splunk_splunk_security_hardening_stig_cat2_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "splunk/splunk_security_hardening/stig_cat2 | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_splunk_splunk_security_hardening_stig_cat2_status: "failed"
        fe_splunk_splunk_security_hardening_stig_cat2_postflight_passed: false
        fe_splunk_splunk_security_hardening_stig_cat2_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_splunk_splunk_security_hardening_stig_cat2_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "splunk/splunk_security_hardening/stig_cat2 | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          splunk/splunk_security_hardening/stig_cat2: FAILED at '{{ fe_splunk_splunk_security_hardening_stig_cat2_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_splunk_splunk_security_hardening_stig_cat2_error }}
      tags:
        - always
        - rescue

    - name: "splunk/splunk_security_hardening/stig_cat2 | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          splunk/splunk_security_hardening/stig_cat2 failed at '{{ fe_splunk_splunk_security_hardening_stig_cat2_failed_task }}': {{ fe_splunk_splunk_security_hardening_stig_cat2_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "splunk/splunk_security_hardening/stig_cat2 | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_splunk_splunk_security_hardening_stig_cat2_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_splunk_splunk_security_hardening_stig_cat2_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_splunk_splunk_security_hardening_stig_cat2_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "splunk/splunk_security_hardening/stig_cat2 | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "splunk/splunk_security_hardening/stig_cat2":
              component: "splunk/splunk_security_hardening/stig_cat2"
              type: "role-tasks"
              platform: "splunk"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_splunk_splunk_security_hardening_stig_cat2_status | default('unknown') }}"
              preflight_passed: "{{ fe_splunk_splunk_security_hardening_stig_cat2_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_splunk_splunk_security_hardening_stig_cat2_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_splunk_splunk_security_hardening_stig_cat2_started_at | default('') }}"
              ended_at: "{{ fe_splunk_splunk_security_hardening_stig_cat2_ended_at | default('') }}"
              duration_seconds: "{{ fe_splunk_splunk_security_hardening_stig_cat2_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_splunk_splunk_security_hardening_stig_cat2_failed_task | default('') }}"
              error: "{{ fe_splunk_splunk_security_hardening_stig_cat2_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_splunk_splunk_security_hardening_stig_cat2_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_splunk_splunk_security_hardening_stig_cat2_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_splunk_splunk_security_hardening_stig_cat2_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_splunk_splunk_security_hardening_stig_cat2_status | default('') == 'succeeded' else ['splunk/splunk_security_hardening/stig_cat2'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "splunk/splunk_security_hardening/stig_cat2 | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_splunk_splunk_security_hardening_stig_cat2_stats_published: true
      tags:
        - always
