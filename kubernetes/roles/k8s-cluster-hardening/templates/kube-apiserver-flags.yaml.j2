# REVIEW REQUIRED before production. Populate k8s_apiserver_extra_flags with the
# hardening flags appropriate for your cluster. See KNOWN_LIMITATIONS.md (#13).
{% for flag in k8s_apiserver_extra_flags | default([]) %}
{{ flag }}
{% endfor %}
