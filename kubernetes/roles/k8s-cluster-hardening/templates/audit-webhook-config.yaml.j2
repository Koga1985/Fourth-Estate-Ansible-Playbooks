# REVIEW REQUIRED before production. Set k8s_audit_webhook_url; render fails fast if unset.
# See KNOWN_LIMITATIONS.md (#13).
apiVersion: v1
kind: Config
clusters:
  - name: audit-webhook
    cluster:
      server: "{{ k8s_audit_webhook_url }}"
contexts:
  - name: audit-webhook
    context:
      cluster: audit-webhook
      user: ""
current-context: audit-webhook
users: []
