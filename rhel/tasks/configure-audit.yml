---
# tasks/configure-audit.yml - Auditd configuration
# DoD STIG V-204494 through V-204644

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['rhel/configure-audit']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_rhel_configure_audit_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "rhel/configure-audit | Initialize validation state"
  ansible.builtin.set_fact:
    fe_rhel_configure_audit_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_rhel_configure_audit_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_rhel_configure_audit_status: "running"
    fe_rhel_configure_audit_preflight_passed: false
    fe_rhel_configure_audit_postflight_passed: false
    fe_rhel_configure_audit_failed_task: ""
    fe_rhel_configure_audit_error: ""
    fe_rhel_configure_audit_stats_published: false
  tags:
    - always

- name: "rhel/configure-audit | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "rhel/configure-audit | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          rhel/configure-audit: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          rhel/configure-audit: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "rhel/configure-audit | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "rhel/configure-audit: required variable '{{ item }}' is not defined."
        success_msg: "rhel/configure-audit: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_rhel_configure_audit_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "rhel/configure-audit | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_rhel_configure_audit_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Install audit packages
      ansible.builtin.package:
        name:
          - audit
          - audispd-plugins
        state: present
      tags:
        - audit

    - name: Enable and start auditd
      ansible.builtin.systemd:
        name: auditd
        enabled: true
        state: started
      tags:
        - audit

    - name: Check audit status
      ansible.builtin.command: auditctl -s
      register: audit_status
      changed_when: false
      tags:
        - audit
        - validation

    - name: Display audit status
      ansible.builtin.debug:
        msg: "{{ audit_status.stdout_lines }}"
      tags:
        - audit
        - validation

    # ----------------------------- postflight -------------------

    - name: "rhel/configure-audit | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_rhel_configure_audit_preflight_passed | bool
          - fe_rhel_configure_audit_status in ['running', 'succeeded']
        fail_msg: >-
          rhel/configure-audit: postflight validation failed - preflight passed
          ={{ fe_rhel_configure_audit_preflight_passed | default(false) }},
          status={{ fe_rhel_configure_audit_status | default('unknown') }}.
        success_msg: "rhel/configure-audit: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "rhel/configure-audit | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_rhel_configure_audit_postflight_passed: true
        fe_rhel_configure_audit_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "rhel/configure-audit | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_rhel_configure_audit_status: "failed"
        fe_rhel_configure_audit_postflight_passed: false
        fe_rhel_configure_audit_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_rhel_configure_audit_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "rhel/configure-audit | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          rhel/configure-audit: FAILED at '{{ fe_rhel_configure_audit_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_rhel_configure_audit_error }}
      tags:
        - always
        - rescue

    - name: "rhel/configure-audit | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          rhel/configure-audit failed at '{{ fe_rhel_configure_audit_failed_task }}': {{ fe_rhel_configure_audit_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "rhel/configure-audit | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_rhel_configure_audit_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_rhel_configure_audit_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_rhel_configure_audit_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "rhel/configure-audit | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "rhel/configure-audit":
              component: "rhel/configure-audit"
              type: "tasks"
              platform: "rhel"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_rhel_configure_audit_status | default('unknown') }}"
              preflight_passed: "{{ fe_rhel_configure_audit_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_rhel_configure_audit_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_rhel_configure_audit_started_at | default('') }}"
              ended_at: "{{ fe_rhel_configure_audit_ended_at | default('') }}"
              duration_seconds: "{{ fe_rhel_configure_audit_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_rhel_configure_audit_failed_task | default('') }}"
              error: "{{ fe_rhel_configure_audit_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_rhel_configure_audit_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_rhel_configure_audit_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_rhel_configure_audit_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_rhel_configure_audit_status | default('') == 'succeeded' else ['rhel/configure-audit'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "rhel/configure-audit | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_rhel_configure_audit_stats_published: true
      tags:
        - always
