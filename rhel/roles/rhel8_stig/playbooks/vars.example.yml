---
# Example overrides for rhel8_stig (-e @vars.example.yml).
apply_changes: false
rhel8_crypto_policy: "FIPS"     # use "DEFAULT:NO-SHA1" in non-FIPS labs
rhel8_faillock:
  deny: 3
  fail_interval: 900
  unlock_time: 0
rhel8_session_tmout: 600
