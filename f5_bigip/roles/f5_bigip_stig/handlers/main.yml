---
# handlers file for f5_bigip_stig

- name: save bigip config
  f5networks.f5_modules.bigip_command:
    commands:
      - "save sys config"
  when:
    - apply_changes | bool
    - save_config_when_changed | bool
  listen: save bigip config
