# NDM SRG / Network Infrastructure Policy Assessment Inventory Example
# Copy this file to 'inventory' and customize for your environment:
#   cp inventory.example inventory
#
# The assessment is read-only and runs entirely on the control node:
#   ansible-playbook roles/ndm_srg_assessment/playbooks/run.yml -i inventory

[localhost]
localhost ansible_connection=local
