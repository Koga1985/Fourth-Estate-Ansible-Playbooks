---
collections:
  - name: servicenow.itsm
    version: ">=2.3.0"
  # Deprecated upstream; 1.0.x is the only release line that exists (there
  # never was a 2.x). The snow_record tasks in this platform still use it;
  # migrating them to servicenow.itsm is the long-term fix.
  - name: servicenow.servicenow
    version: ">=1.0.0"
  - name: community.general
    version: ">=7.0.0"

  # Used by this platform's roles/tasks; previously undeclared here.
  - name: amazon.aws
    version: ">=7.0.0"

  # Used by this platform's roles/tasks; previously undeclared here.
  - name: azure.azcollection
    version: ">=2.1.0"

  # Used by this platform's roles/tasks; previously undeclared here.
  - name: community.aws
    version: ">=7.0.0"

  # Used by this platform's roles/tasks; previously undeclared here.
  - name: community.vmware
    version: ">=5.0.0"

  # Used by this platform's roles/tasks; previously undeclared here.
  - name: kubernetes.core
    version: ">=2.4.0"
