---
# Example overrides for cisco_ise_stig (-e @vars.example.yml).
apply_changes: false

ise_host: ise.example.mil
ise_username: ersadmin
ise_password: "{{ vault_ise_ers_password }}"
ise_validate_certs: true

# ------------------------------------------------------------
# Data-driven OpenAPI enforcement operations (apply_changes=true only).
# Validate paths/bodies against your ISE version's OpenAPI explorer at
# https://{{ise_host}}/api/swagger before enabling. Empty = assessment only.
# ------------------------------------------------------------
ise_stig_operations: []
#
# Example - set the administrative access GUI session timeout (CISC-ND-000280)
# via the ISE OpenAPI (path/body vary by ISE release; confirm in swagger):
#
# ise_stig_operations:
#   - id: "CISC-ND-000280"
#     title: "Admin GUI session timeout"
#     method: PUT
#     path: "v1/system-settings/gui/session-timeout"
#     status_code: [200, 204]
#     body:
#       sessionTimeout: 10
