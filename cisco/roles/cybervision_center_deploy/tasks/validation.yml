---
# tasks file for cybervision_center_deploy - Post-deployment validation

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['cisco/cybervision_center_deploy/validation']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_cisco_cybervision_center_deploy_validation_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "cisco/cybervision_center_deploy/validation | Initialize validation state"
  ansible.builtin.set_fact:
    fe_cisco_cybervision_center_deploy_validation_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_cisco_cybervision_center_deploy_validation_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_cisco_cybervision_center_deploy_validation_status: "running"
    fe_cisco_cybervision_center_deploy_validation_preflight_passed: false
    fe_cisco_cybervision_center_deploy_validation_postflight_passed: false
    fe_cisco_cybervision_center_deploy_validation_failed_task: ""
    fe_cisco_cybervision_center_deploy_validation_error: ""
    fe_cisco_cybervision_center_deploy_validation_stats_published: false
  tags:
    - always

- name: "cisco/cybervision_center_deploy/validation | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "cisco/cybervision_center_deploy/validation | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          cisco/cybervision_center_deploy/validation: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          cisco/cybervision_center_deploy/validation: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "cisco/cybervision_center_deploy/validation | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "cisco/cybervision_center_deploy/validation: required variable '{{ item }}' is not defined."
        success_msg: "cisco/cybervision_center_deploy/validation: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_cisco_cybervision_center_deploy_validation_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "cisco/cybervision_center_deploy/validation | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_cisco_cybervision_center_deploy_validation_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: PHASE 1 - Validate Center API is responsive
      ansible.builtin.uri:
        url: "{{ cv_api_url }}/system/version"
        method: GET
        headers:
          Authorization: "Bearer {{ cv_api_token }}"
          Accept: "application/json"
        validate_certs: "{{ cv_validate_certs }}"
        timeout: "{{ cv_timeout }}"
        status_code: 200
      register: cv_validate_api

    - name: PHASE 1 - Validate license is active
      ansible.builtin.uri:
        url: "{{ cv_api_url }}/system/license"
        method: GET
        headers:
          Authorization: "Bearer {{ cv_api_token }}"
          Accept: "application/json"
        validate_certs: "{{ cv_validate_certs }}"
        timeout: "{{ cv_timeout }}"
        status_code: 200
      register: cv_validate_license
      failed_when: false

    - name: PHASE 1 - Validate enrolled sensor count
      ansible.builtin.uri:
        url: "{{ cv_api_url }}/sensors"
        method: GET
        headers:
          Authorization: "Bearer {{ cv_api_token }}"
          Accept: "application/json"
        validate_certs: "{{ cv_validate_certs }}"
        timeout: "{{ cv_timeout }}"
        status_code: 200
      register: cv_validate_sensors
      failed_when: false

    - name: Save validation artifact
      ansible.builtin.copy:
        content: "{{ validation_artifact | to_nice_json }}"
        dest: "{{ artifacts_dir }}/cv_center_validation.json"
        mode: '0640'
      vars:
        validation_artifact:
          timestamp: "{{ ansible_date_time.iso8601 }}"
          center_host: "{{ cv_center_host }}"
          api_responsive: "{{ cv_validate_api.status == 200 }}"
          version: "{{ cv_validate_api.json.version | default('unknown') }}"
          license_status: "{{ cv_validate_license.json.status | default('unknown') }}"
          sensor_count: "{{ cv_validate_sensors.json | default([]) | length }}"

    - name: Display validation summary
      ansible.builtin.debug:
        msg:
          - "================================================================"
          - " Cyber Vision Center - Validation Summary"
          - "================================================================"
          - " API responsive:   {{ cv_validate_api.status == 200 }}"
          - " Version:          {{ cv_validate_api.json.version | default('unknown') }}"
          - " License status:   {{ cv_validate_license.json.status | default('unknown') }}"
          - " Enrolled sensors: {{ cv_validate_sensors.json | default([]) | length }}"
          - "================================================================"

    # ----------------------------- postflight -------------------

    - name: "cisco/cybervision_center_deploy/validation | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_cisco_cybervision_center_deploy_validation_preflight_passed | bool
          - fe_cisco_cybervision_center_deploy_validation_status in ['running', 'succeeded']
        fail_msg: >-
          cisco/cybervision_center_deploy/validation: postflight validation failed - preflight passed
          ={{ fe_cisco_cybervision_center_deploy_validation_preflight_passed | default(false) }},
          status={{ fe_cisco_cybervision_center_deploy_validation_status | default('unknown') }}.
        success_msg: "cisco/cybervision_center_deploy/validation: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "cisco/cybervision_center_deploy/validation | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_cisco_cybervision_center_deploy_validation_postflight_passed: true
        fe_cisco_cybervision_center_deploy_validation_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "cisco/cybervision_center_deploy/validation | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_cisco_cybervision_center_deploy_validation_status: "failed"
        fe_cisco_cybervision_center_deploy_validation_postflight_passed: false
        fe_cisco_cybervision_center_deploy_validation_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_cisco_cybervision_center_deploy_validation_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "cisco/cybervision_center_deploy/validation | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          cisco/cybervision_center_deploy/validation: FAILED at '{{ fe_cisco_cybervision_center_deploy_validation_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_cisco_cybervision_center_deploy_validation_error }}
      tags:
        - always
        - rescue

    - name: "cisco/cybervision_center_deploy/validation | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          cisco/cybervision_center_deploy/validation failed at '{{ fe_cisco_cybervision_center_deploy_validation_failed_task }}': {{ fe_cisco_cybervision_center_deploy_validation_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "cisco/cybervision_center_deploy/validation | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_cisco_cybervision_center_deploy_validation_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_cisco_cybervision_center_deploy_validation_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_cisco_cybervision_center_deploy_validation_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "cisco/cybervision_center_deploy/validation | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "cisco/cybervision_center_deploy/validation":
              component: "cisco/cybervision_center_deploy/validation"
              type: "role-tasks"
              platform: "cisco"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_cisco_cybervision_center_deploy_validation_status | default('unknown') }}"
              preflight_passed: "{{ fe_cisco_cybervision_center_deploy_validation_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_cisco_cybervision_center_deploy_validation_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_cisco_cybervision_center_deploy_validation_started_at | default('') }}"
              ended_at: "{{ fe_cisco_cybervision_center_deploy_validation_ended_at | default('') }}"
              duration_seconds: "{{ fe_cisco_cybervision_center_deploy_validation_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_cisco_cybervision_center_deploy_validation_failed_task | default('') }}"
              error: "{{ fe_cisco_cybervision_center_deploy_validation_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_cisco_cybervision_center_deploy_validation_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_cisco_cybervision_center_deploy_validation_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_cisco_cybervision_center_deploy_validation_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_cisco_cybervision_center_deploy_validation_status | default('') == 'succeeded' else ['cisco/cybervision_center_deploy/validation'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "cisco/cybervision_center_deploy/validation | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_cisco_cybervision_center_deploy_validation_stats_published: true
      tags:
        - always
