---
# defaults file for ise_report__endpoint_catalog
# Cisco ISE endpoint catalog and inventory reporting

# ISE Connection Parameters
ise_hostname: "{{ vault_ise_hostname }}"
ise_username: "{{ vault_ise_username }}"
ise_password: "{{ vault_ise_password }}"
ise_verify_ssl: true
ise_use_proxy: false
ise_debug: false

# Deployment Control
apply_changes: false
ise_artifacts_dir: "/tmp/ise-artifacts"

# Fourth Estate Configuration
fourth_estate_org: "FourthEstate"
fourth_estate_contact: "{{ vault_fourth_estate_contact }}"

# Feature Configuration
ise_report__endpoint_catalog_enabled: true

# DISA STIG Compliance
enable_disa_stig_compliance: true

# Compliance Frameworks
compliance_frameworks:
  - "dod_stig"
  - "nist_800_53"
  - "nist_800_171"
  - "fisma_moderate"

# Logging
ise_report__endpoint_catalog_log_level: "INFO"
ise_report__endpoint_catalog_log_to_syslog: true
ise_report__endpoint_catalog_syslog_server: "{{ vault_syslog_server }}"

# Notification Settings
ise_report__endpoint_catalog_notify_on_completion: false
ise_report__endpoint_catalog_notification_email: "{{ vault_security_team_email }}"

# Backup Settings
ise_report__endpoint_catalog_auto_backup: true
