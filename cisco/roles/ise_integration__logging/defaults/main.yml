---
# defaults file for ise_integration__logging
# Cisco ISE logging and SIEM integration

# ISE Connection Parameters
ise_hostname: "{{ vault_ise_hostname }}"
ise_username: "{{ vault_ise_username }}"
ise_password: "{{ vault_ise_password }}"
ise_verify_ssl: true
ise_use_proxy: false
ise_debug: false

# Deployment Control
apply_changes: false
ise_artifacts_dir: "/tmp/ise-artifacts"

# Fourth Estate Configuration
fourth_estate_org: "FourthEstate"
fourth_estate_contact: "{{ vault_fourth_estate_contact }}"

# Feature Configuration
ise_integration__logging_enabled: true

# DISA STIG Compliance
enable_disa_stig_compliance: true

# Compliance Frameworks
compliance_frameworks:
  - "dod_stig"
  - "nist_800_53"
  - "nist_800_171"
  - "fisma_moderate"

# Logging
ise_integration__logging_log_level: "INFO"
ise_integration__logging_log_to_syslog: true
ise_integration__logging_syslog_server: "{{ vault_syslog_server }}"

# Notification Settings
ise_integration__logging_notify_on_completion: false
ise_integration__logging_notification_email: "{{ vault_security_team_email }}"

# Backup Settings
ise_integration__logging_auto_backup: true
