{
  "report_type": "ucs_networking",
  "generated_at": "{{ ansible_date_time.iso8601 }}",
  "ucs_domain": "{{ ucs_hostname }}",
  "vlans_configured": {{ ucs_vlans | default([]) | length }},
  "vsans_configured": {{ ucs_vsans | default([]) | length }},
  "qos_policies": {{ ucs_qos_policies | default([]) | length }},
  "configuration": {
    "san_enabled": {{ ucs_enable_san | default(false) | lower }},
    "iscsi_enabled": {{ ucs_enable_iscsi | default(false) | lower }}
  }
}
