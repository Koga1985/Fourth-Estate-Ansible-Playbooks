---
# handlers file for cisco_asa_stig

- name: write memory
  cisco.asa.asa_config:
    save_when: modified
  when:
    - apply_changes | bool
    - save_config_when_changed | bool
  listen: write memory
