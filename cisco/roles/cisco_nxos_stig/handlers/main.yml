---
# handlers file for cisco_nxos_stig

- name: save running-config
  cisco.nxos.nxos_config:
    save_when: modified
  when:
    - apply_changes | bool
    - save_config_when_changed | bool
  listen: save running-config
