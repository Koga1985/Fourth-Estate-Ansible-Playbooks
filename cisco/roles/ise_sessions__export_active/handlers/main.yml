---
# handlers file for ise_sessions__export_active

- name: backup ise configuration
  cisco.ise.backup_last_status_info:
    ise_hostname: "{{ ise_hostname }}"
    ise_username: "{{ ise_username }}"
    ise_password: "{{ ise_password }}"
    ise_verify: "{{ ise_verify_ssl }}"
  when: ise_auto_backup | default(false) | bool

- name: send notification
  ansible.builtin.mail:
    host: "{{ smtp_host | default('localhost') }}"
    port: "{{ smtp_port | default(25) }}"
    to: "{{ ise_sessions__export_active_notification_email }}"
    subject: "ISE ise_sessions__export_active Configuration Update"
    body: "ise_sessions__export_active has been updated on {{ ise_hostname }} at {{ ansible_date_time.iso8601 }}"
  when: ise_sessions__export_active_notify_on_completion | bool
  ignore_errors: true

- name: log changes to syslog
  ansible.builtin.syslog:
    msg: "ISE ise_sessions__export_active configuration updated"
    priority: "info"
    facility: "local0"
  when: ise_sessions__export_active_log_to_syslog | bool
