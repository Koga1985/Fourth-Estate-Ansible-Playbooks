
---
# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# Every play below carries a block/rescue/always validation wrapper:
#
#   pre_tasks   preflight validation. On failure it records the reason,
#               publishes the outcome with set_stats and aborts the play
#               before any role or task runs.
#   tasks       the original task list, wrapped so a failure is captured
#               (which task, which error) and then re-raised.
#   post_tasks  postflight validation, then the run record is published with
#               ansible.builtin.set_stats.
#
# Set fe_validation_continue_on_error=true to record failures and keep going.
# See docs/VALIDATION_AND_STATS.md for the published stats schema.
# ---------------------------------------------------------------------------

- name: ise_idstore__ad_join — AD join
  hosts: localhost
  connection: local
  gather_facts: false
  collections:
    - cisco.ise
    - community.general
  vars:
    artifacts_dir: "{{ artifacts_dir | default('/tmp/ise-artifacts') }}"
    apply_changes: "{{ apply_changes | default(false) }}"

    ad_domain: ""
    ad_username: ""
    ad_password: ""
    dns_servers: []
    advanced:
      site: ""
      dnsSRV: true
  tasks:
    - name: "cisco/tasks/ise_idstore__ad_join | Guarded execution with rescue and always handling"
      block:

        - name: Emit plan
          ansible.builtin.copy:
            dest: "{{ artifacts_dir }}/ad_join_plan.json"
            mode: "0644"
            content: "{{ {'domain': ad_domain, 'dns': dns_servers, 'advanced': advanced} | to_nice_json }}"
        - name: Join AD (placeholder)
          when: apply_changes | bool
          ansible.builtin.debug:
            msg: "Join domain {{ ad_domain }} via ISE AD join endpoints."

        - name: "cisco/tasks/ise_idstore__ad_join | Flush notified handlers inside the guarded block"
          ansible.builtin.meta: flush_handlers
          tags:
            - always

      rescue:

        - name: "cisco/tasks/ise_idstore__ad_join | Rescue | Capture the failing task and error detail"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_status: "failed"
            fe_cisco_tasks_ise_idstore__ad_join_postflight_passed: false
            fe_cisco_tasks_ise_idstore__ad_join_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
            fe_cisco_tasks_ise_idstore__ad_join_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
          tags:
            - always
            - rescue

        - name: "cisco/tasks/ise_idstore__ad_join | Rescue | Report the failure"
          ansible.builtin.debug:
            msg: >-
              cisco/tasks/ise_idstore__ad_join: FAILED at '{{ fe_cisco_tasks_ise_idstore__ad_join_failed_task }}' on
              {{ inventory_hostname }} - {{ fe_cisco_tasks_ise_idstore__ad_join_error }}
          tags:
            - always
            - rescue

        - name: "cisco/tasks/ise_idstore__ad_join | Rescue | Re-raise the failure unless the caller opted to continue"
          ansible.builtin.fail:
            msg: >-
              cisco/tasks/ise_idstore__ad_join failed at '{{ fe_cisco_tasks_ise_idstore__ad_join_failed_task }}': {{ fe_cisco_tasks_ise_idstore__ad_join_error }}
          when: not (fe_validation_continue_on_error | default(false) | bool)
          tags:
            - always
            - rescue

      always:

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Compute the execution duration"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
            fe_cisco_tasks_ise_idstore__ad_join_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_cisco_tasks_ise_idstore__ad_join_start_epoch | default(0) | int) }}"
          tags:
            - always

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Publish the validation result with set_stats"
          ansible.builtin.set_stats:
            data:
              fe_validation_results:
                "cisco/tasks/ise_idstore__ad_join":
                  component: "cisco/tasks/ise_idstore__ad_join"
                  type: "playbook"
                  platform: "cisco"
                  host: "{{ inventory_hostname }}"
                  status: "{{ fe_cisco_tasks_ise_idstore__ad_join_status | default('unknown') }}"
                  preflight_passed: "{{ fe_cisco_tasks_ise_idstore__ad_join_preflight_passed | default(false) | bool }}"
                  postflight_passed: "{{ fe_cisco_tasks_ise_idstore__ad_join_postflight_passed | default(false) | bool }}"
                  started_at: "{{ fe_cisco_tasks_ise_idstore__ad_join_started_at | default('') }}"
                  ended_at: "{{ fe_cisco_tasks_ise_idstore__ad_join_ended_at | default('') }}"
                  duration_seconds: "{{ fe_cisco_tasks_ise_idstore__ad_join_duration_seconds | default(0) | int }}"
                  failed_task: "{{ fe_cisco_tasks_ise_idstore__ad_join_failed_task | default('') }}"
                  error: "{{ fe_cisco_tasks_ise_idstore__ad_join_error | default('') }}"
                  check_mode: "{{ ansible_check_mode | default(false) | bool }}"
              fe_validation_executions: 1
              fe_validation_succeeded: "{{ 1 if fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'succeeded' else 0 }}"
              fe_validation_failed: "{{ 0 if fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'succeeded' else 1 }}"
              fe_validation_duration_seconds: "{{ fe_cisco_tasks_ise_idstore__ad_join_duration_seconds | default(0) | int }}"
              fe_validation_failed_components: >-
                {{ [] if fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'succeeded' else ['cisco/tasks/ise_idstore__ad_join'] }}
            aggregate: true
            per_host: false
          when:
            - fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'failed'
            - not (fe_cisco_tasks_ise_idstore__ad_join_stats_published | default(false) | bool)
          tags:
            - always

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Mark the validation statistics as published"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_stats_published: true
          when:
            - fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'failed'
          tags:
            - always
  pre_tasks:
    - name: "cisco/tasks/ise_idstore__ad_join | Preflight validation"
      tags:
        - always
        - preflight
        - validation
      block:

        - name: "cisco/tasks/ise_idstore__ad_join | Initialize validation state"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_start_epoch: "{{ now(fmt='%s') | int }}"
            fe_cisco_tasks_ise_idstore__ad_join_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
            fe_cisco_tasks_ise_idstore__ad_join_status: "running"
            fe_cisco_tasks_ise_idstore__ad_join_preflight_passed: false
            fe_cisco_tasks_ise_idstore__ad_join_postflight_passed: false
            fe_cisco_tasks_ise_idstore__ad_join_failed_task: ""
            fe_cisco_tasks_ise_idstore__ad_join_error: ""
            fe_cisco_tasks_ise_idstore__ad_join_stats_published: false
          tags:
            - always

        - name: "cisco/tasks/ise_idstore__ad_join | Preflight | Verify the control node meets the minimum ansible-core version"
          ansible.builtin.assert:
            that:
              - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
            fail_msg: >-
              cisco/tasks/ise_idstore__ad_join: requires ansible-core
              {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
              found {{ ansible_version.full }}.
            success_msg: >-
              cisco/tasks/ise_idstore__ad_join: ansible-core {{ ansible_version.full }} satisfies the minimum.
            quiet: true
          tags:
            - always
            - preflight
            - validation

        - name: "cisco/tasks/ise_idstore__ad_join | Preflight | Verify every declared required variable is defined"
          ansible.builtin.assert:
            that:
              - vars[item] is defined
            fail_msg: "cisco/tasks/ise_idstore__ad_join: required variable '{{ item }}' is not defined."
            success_msg: "cisco/tasks/ise_idstore__ad_join: required variable '{{ item }}' is defined."
            quiet: true
          loop: "{{ fe_cisco_tasks_ise_idstore__ad_join_required_vars | default([]) }}"
          loop_control:
            label: "{{ item }}"
          tags:
            - always
            - preflight
            - validation

        - name: "cisco/tasks/ise_idstore__ad_join | Preflight | Record that preflight validation passed"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_preflight_passed: true
          tags:
            - always
            - preflight
            - validation

      rescue:

        - name: "cisco/tasks/ise_idstore__ad_join | Rescue | Capture the failing task and error detail"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_status: "preflight_failed"
            fe_cisco_tasks_ise_idstore__ad_join_postflight_passed: false
            fe_cisco_tasks_ise_idstore__ad_join_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
            fe_cisco_tasks_ise_idstore__ad_join_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
          tags:
            - always
            - rescue

        - name: "cisco/tasks/ise_idstore__ad_join | Rescue | Report the failure"
          ansible.builtin.debug:
            msg: >-
              cisco/tasks/ise_idstore__ad_join: FAILED at '{{ fe_cisco_tasks_ise_idstore__ad_join_failed_task }}' on
              {{ inventory_hostname }} - {{ fe_cisco_tasks_ise_idstore__ad_join_error }}
          tags:
            - always
            - rescue

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Publish the validation result with set_stats"
          ansible.builtin.set_stats:
            data:
              fe_validation_results:
                "cisco/tasks/ise_idstore__ad_join":
                  component: "cisco/tasks/ise_idstore__ad_join"
                  type: "playbook"
                  platform: "cisco"
                  host: "{{ inventory_hostname }}"
                  status: "{{ fe_cisco_tasks_ise_idstore__ad_join_status | default('unknown') }}"
                  preflight_passed: "{{ fe_cisco_tasks_ise_idstore__ad_join_preflight_passed | default(false) | bool }}"
                  postflight_passed: "{{ fe_cisco_tasks_ise_idstore__ad_join_postflight_passed | default(false) | bool }}"
                  started_at: "{{ fe_cisco_tasks_ise_idstore__ad_join_started_at | default('') }}"
                  ended_at: "{{ fe_cisco_tasks_ise_idstore__ad_join_ended_at | default('') }}"
                  duration_seconds: "{{ fe_cisco_tasks_ise_idstore__ad_join_duration_seconds | default(0) | int }}"
                  failed_task: "{{ fe_cisco_tasks_ise_idstore__ad_join_failed_task | default('') }}"
                  error: "{{ fe_cisco_tasks_ise_idstore__ad_join_error | default('') }}"
                  check_mode: "{{ ansible_check_mode | default(false) | bool }}"
              fe_validation_executions: 1
              fe_validation_succeeded: "{{ 1 if fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'succeeded' else 0 }}"
              fe_validation_failed: "{{ 0 if fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'succeeded' else 1 }}"
              fe_validation_duration_seconds: "{{ fe_cisco_tasks_ise_idstore__ad_join_duration_seconds | default(0) | int }}"
              fe_validation_failed_components: >-
                {{ [] if fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'succeeded' else ['cisco/tasks/ise_idstore__ad_join'] }}
            aggregate: true
            per_host: false
          tags:
            - always

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Mark the validation statistics as published"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_stats_published: true
          tags:
            - always

        - name: "cisco/tasks/ise_idstore__ad_join | Rescue | Re-raise the failure unless the caller opted to continue"
          ansible.builtin.fail:
            msg: >-
              cisco/tasks/ise_idstore__ad_join failed at '{{ fe_cisco_tasks_ise_idstore__ad_join_failed_task }}': {{ fe_cisco_tasks_ise_idstore__ad_join_error }}
          when: not (fe_validation_continue_on_error | default(false) | bool)
          tags:
            - always
            - rescue

      always:

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Report the preflight outcome"
          ansible.builtin.debug:
            msg: >-
              cisco/tasks/ise_idstore__ad_join: preflight
              {{ 'passed' if fe_cisco_tasks_ise_idstore__ad_join_preflight_passed | default(false) | bool else 'FAILED' }}
              on {{ inventory_hostname }}.
          tags:
            - always
  post_tasks:
    - name: "cisco/tasks/ise_idstore__ad_join | Postflight validation"
      tags:
        - always
        - postflight
        - validation
      block:

        - name: "cisco/tasks/ise_idstore__ad_join | Postflight | Verify the guarded body ran after a passing preflight"
          ansible.builtin.assert:
            that:
              - fe_cisco_tasks_ise_idstore__ad_join_preflight_passed | bool
              - fe_cisco_tasks_ise_idstore__ad_join_status in ['running', 'succeeded']
            fail_msg: >-
              cisco/tasks/ise_idstore__ad_join: postflight validation failed - preflight passed
              ={{ fe_cisco_tasks_ise_idstore__ad_join_preflight_passed | default(false) }},
              status={{ fe_cisco_tasks_ise_idstore__ad_join_status | default('unknown') }}.
            success_msg: "cisco/tasks/ise_idstore__ad_join: postflight validation passed."
            quiet: true
          tags:
            - always
            - postflight
            - validation

        - name: "cisco/tasks/ise_idstore__ad_join | Postflight | Record the successful outcome"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_postflight_passed: true
            fe_cisco_tasks_ise_idstore__ad_join_status: "succeeded"
          tags:
            - always
            - postflight
            - validation

      rescue:

        - name: "cisco/tasks/ise_idstore__ad_join | Rescue | Capture the failing task and error detail"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_status: "failed"
            fe_cisco_tasks_ise_idstore__ad_join_postflight_passed: false
            fe_cisco_tasks_ise_idstore__ad_join_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
            fe_cisco_tasks_ise_idstore__ad_join_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
          tags:
            - always
            - rescue

        - name: "cisco/tasks/ise_idstore__ad_join | Rescue | Report the failure"
          ansible.builtin.debug:
            msg: >-
              cisco/tasks/ise_idstore__ad_join: FAILED at '{{ fe_cisco_tasks_ise_idstore__ad_join_failed_task }}' on
              {{ inventory_hostname }} - {{ fe_cisco_tasks_ise_idstore__ad_join_error }}
          tags:
            - always
            - rescue

        - name: "cisco/tasks/ise_idstore__ad_join | Rescue | Re-raise the failure unless the caller opted to continue"
          ansible.builtin.fail:
            msg: >-
              cisco/tasks/ise_idstore__ad_join failed at '{{ fe_cisco_tasks_ise_idstore__ad_join_failed_task }}': {{ fe_cisco_tasks_ise_idstore__ad_join_error }}
          when: not (fe_validation_continue_on_error | default(false) | bool)
          tags:
            - always
            - rescue

      always:

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Compute the execution duration"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
            fe_cisco_tasks_ise_idstore__ad_join_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_cisco_tasks_ise_idstore__ad_join_start_epoch | default(0) | int) }}"
          tags:
            - always

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Publish the validation result with set_stats"
          ansible.builtin.set_stats:
            data:
              fe_validation_results:
                "cisco/tasks/ise_idstore__ad_join":
                  component: "cisco/tasks/ise_idstore__ad_join"
                  type: "playbook"
                  platform: "cisco"
                  host: "{{ inventory_hostname }}"
                  status: "{{ fe_cisco_tasks_ise_idstore__ad_join_status | default('unknown') }}"
                  preflight_passed: "{{ fe_cisco_tasks_ise_idstore__ad_join_preflight_passed | default(false) | bool }}"
                  postflight_passed: "{{ fe_cisco_tasks_ise_idstore__ad_join_postflight_passed | default(false) | bool }}"
                  started_at: "{{ fe_cisco_tasks_ise_idstore__ad_join_started_at | default('') }}"
                  ended_at: "{{ fe_cisco_tasks_ise_idstore__ad_join_ended_at | default('') }}"
                  duration_seconds: "{{ fe_cisco_tasks_ise_idstore__ad_join_duration_seconds | default(0) | int }}"
                  failed_task: "{{ fe_cisco_tasks_ise_idstore__ad_join_failed_task | default('') }}"
                  error: "{{ fe_cisco_tasks_ise_idstore__ad_join_error | default('') }}"
                  check_mode: "{{ ansible_check_mode | default(false) | bool }}"
              fe_validation_executions: 1
              fe_validation_succeeded: "{{ 1 if fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'succeeded' else 0 }}"
              fe_validation_failed: "{{ 0 if fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'succeeded' else 1 }}"
              fe_validation_duration_seconds: "{{ fe_cisco_tasks_ise_idstore__ad_join_duration_seconds | default(0) | int }}"
              fe_validation_failed_components: >-
                {{ [] if fe_cisco_tasks_ise_idstore__ad_join_status | default('') == 'succeeded' else ['cisco/tasks/ise_idstore__ad_join'] }}
            aggregate: true
            per_host: false
          when:
            - not (fe_cisco_tasks_ise_idstore__ad_join_stats_published | default(false) | bool)
          tags:
            - always

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Mark the validation statistics as published"
          ansible.builtin.set_fact:
            fe_cisco_tasks_ise_idstore__ad_join_stats_published: true
          tags:
            - always

        - name: "cisco/tasks/ise_idstore__ad_join | Always | Report the run summary"
          ansible.builtin.debug:
            msg: >-
              cisco/tasks/ise_idstore__ad_join: {{ fe_cisco_tasks_ise_idstore__ad_join_status | default('unknown') }} in
              {{ fe_cisco_tasks_ise_idstore__ad_join_duration_seconds | default(0) }}s on {{ inventory_hostname }}.
          tags:
            - always
