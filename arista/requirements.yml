---
# Arista Networks Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: arista.eos
    version: ">=6.0.0"

  - name: arista.cvp
    version: ">=3.8.0"

  - name: community.general
    version: ">=8.0.0"

  - name: ansible.netcommon
    version: ">=5.0.0"

# Python dependencies:
# pip install cvprac pyeapi
