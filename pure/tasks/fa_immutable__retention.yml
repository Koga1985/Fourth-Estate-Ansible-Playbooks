---
# vars example:
# immutability:
#   enable: false
#   retention_days: 30
#   approval_required: true
- name: Configure immutability (SafeMode) — placeholder/guarded
  ansible.builtin.debug:
    msg: "SafeMode settings often require support engagement; validate tenant/version. Requested: {{ immutability | default({}) }}"
