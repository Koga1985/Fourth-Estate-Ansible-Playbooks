# System packages for the Fourth Estate execution environment.
# Format: https://docs.opendev.org/opendev/bindep/latest/readme.html

# Toolchain for python packages that compile native extensions
gcc [compile platform:rpm]
python3-devel [compile platform:rpm]

# Kerberos auth for pywinrm[kerberos] (Windows / AD environments)
krb5-devel [platform:rpm]
krb5-workstation [platform:rpm]

# ansible-pylibssh / paramiko build and runtime deps
libssh-devel [compile platform:rpm]
openssl-devel [compile platform:rpm]

# Password-based SSH fallback used by some network bootstrap flows
sshpass [platform:rpm]

# Roles that clone or archive content
git-core [platform:rpm]
tar [platform:rpm]
unzip [platform:rpm]
