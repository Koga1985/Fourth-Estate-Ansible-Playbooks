# Python dependencies for the Fourth Estate execution environment that are
# NOT declared by the collections themselves. ansible-builder introspects
# each collection's own requirements.txt at build time, so cloud SDKs
# (boto3, azure-*, google-*) and vendor clients (py-pure-client, pyvmomi,
# hvac, ...) arrive via that mechanism -- do not duplicate them here.

# Windows hosts over WinRM (connection plugin dependency, not a collection dep)
pywinrm>=0.4.3

# Kerberos auth for domain-joined Windows hosts (needs krb5-devel; see bindep.txt)
pywinrm[kerberos]>=0.4.3

# Common filter/lookup dependencies used across this repo's roles
netaddr
jmespath
dnspython

# Network CLI transports (cisco.ios / nxos / arista.eos over network_cli)
paramiko
ansible-pylibssh
