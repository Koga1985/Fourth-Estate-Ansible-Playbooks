galaxy_info:
  author: Dewain Smith
  description: Configure an ESXi host standalone or join/configure it via vCenter (DNS/NTP/syslog/services/network) and optionally add it to a Datacenter/Cluster.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions: ["all"]
    - name: Ubuntu
      versions: ["all"]
  galaxy_tags: ["vmware","vsphere","esxi","host","configuration"]
dependencies: []
