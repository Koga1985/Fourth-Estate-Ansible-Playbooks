---
# VMware Ansible Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: community.vmware
    version: ">=4.0.0"

  - name: vmware.vmware
    version: ">=1.0.0"

  - name: vmware.vmware_rest
    version: ">=3.0.0"

  - name: community.general
    version: ">=8.0.0"

  - name: ansible.posix
    version: ">=1.5.0"

# Python dependencies (install separately):
# pip install pyvmomi vSphere-Automation-SDK requests
# pip install aiohttp  # For vmware_rest collection
