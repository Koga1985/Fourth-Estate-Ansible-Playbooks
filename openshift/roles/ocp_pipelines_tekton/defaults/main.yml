---

# Common connection settings for kubernetes.core.k8s
kubeconfig: "{{ lookup('env','KUBECONFIG') | default('~/.kube/config', true) }}"
context: ""
namespace: "openshift-config"
apply_wait: true
artifacts_dir: "/tmp/ocp-artifacts"


tekton_manifests: []      # Operator/Subscription/TektonConfig/TektonChain CRs
tekton_resources: []      # Pipelines/Tasks/Triggers/Secrets/PVCs

