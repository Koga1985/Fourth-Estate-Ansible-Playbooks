---
# Example overrides for win_iis10_stig (-e @vars.example.yml).
apply_changes: false

iis_site_name: "Default Web Site"
iis_app_pool_name: "DefaultAppPool"
iis_session_timeout_minutes: 20

iis_disable_protocols:
  - "SSL 2.0"
  - "SSL 3.0"
  - "TLS 1.0"
  - "TLS 1.1"
iis_enable_protocols:
  - "TLS 1.2"
