---
# vars:
# vol_renames:
#   - { name: app01-dbtemp, new_name: app01-db01, tags: { env: prod, role: db } }

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['pure_storage/fa_vol__rename_tag']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_pure_storage_fa_vol__rename_tag_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "pure_storage/fa_vol__rename_tag | Initialize validation state"
  ansible.builtin.set_fact:
    fe_pure_storage_fa_vol__rename_tag_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_pure_storage_fa_vol__rename_tag_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_pure_storage_fa_vol__rename_tag_status: "running"
    fe_pure_storage_fa_vol__rename_tag_preflight_passed: false
    fe_pure_storage_fa_vol__rename_tag_postflight_passed: false
    fe_pure_storage_fa_vol__rename_tag_failed_task: ""
    fe_pure_storage_fa_vol__rename_tag_error: ""
    fe_pure_storage_fa_vol__rename_tag_stats_published: false
  tags:
    - always

- name: "pure_storage/fa_vol__rename_tag | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "pure_storage/fa_vol__rename_tag | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          pure_storage/fa_vol__rename_tag: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          pure_storage/fa_vol__rename_tag: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "pure_storage/fa_vol__rename_tag | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "pure_storage/fa_vol__rename_tag: required variable '{{ item }}' is not defined."
        success_msg: "pure_storage/fa_vol__rename_tag: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_pure_storage_fa_vol__rename_tag_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "pure_storage/fa_vol__rename_tag | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_pure_storage_fa_vol__rename_tag_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Rename volumes (if needed)
      purestorage.flasharray.purefa_volume:
        fa_url: "{{ fa_url }}"
        api_token: "{{ api_token }}"
        name: "{{ item.name }}"
        new_name: "{{ item.new_name }}"
        state: present
      loop: "{{ vol_renames | default([]) }}"
      loop_control: { label: "{{ item.name }} -> {{ item.new_name }}" }

    - name: Apply/Update tags (labels)
      purestorage.flasharray.purefa_volume:
        fa_url: "{{ fa_url }}"
        api_token: "{{ api_token }}"
        name: "{{ item.new_name | default(item.name) }}"
        labels: "{{ item.tags | default(omit) }}"
        state: present
      loop: "{{ vol_renames | default([]) }}"
      loop_control: { label: "{{ item.new_name | default(item.name) }}" }

    # ----------------------------- postflight -------------------

    - name: "pure_storage/fa_vol__rename_tag | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_pure_storage_fa_vol__rename_tag_preflight_passed | bool
          - fe_pure_storage_fa_vol__rename_tag_status in ['running', 'succeeded']
        fail_msg: >-
          pure_storage/fa_vol__rename_tag: postflight validation failed - preflight passed
          ={{ fe_pure_storage_fa_vol__rename_tag_preflight_passed | default(false) }},
          status={{ fe_pure_storage_fa_vol__rename_tag_status | default('unknown') }}.
        success_msg: "pure_storage/fa_vol__rename_tag: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "pure_storage/fa_vol__rename_tag | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_pure_storage_fa_vol__rename_tag_postflight_passed: true
        fe_pure_storage_fa_vol__rename_tag_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "pure_storage/fa_vol__rename_tag | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_pure_storage_fa_vol__rename_tag_status: "failed"
        fe_pure_storage_fa_vol__rename_tag_postflight_passed: false
        fe_pure_storage_fa_vol__rename_tag_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_pure_storage_fa_vol__rename_tag_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "pure_storage/fa_vol__rename_tag | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          pure_storage/fa_vol__rename_tag: FAILED at '{{ fe_pure_storage_fa_vol__rename_tag_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_pure_storage_fa_vol__rename_tag_error }}
      tags:
        - always
        - rescue

    - name: "pure_storage/fa_vol__rename_tag | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          pure_storage/fa_vol__rename_tag failed at '{{ fe_pure_storage_fa_vol__rename_tag_failed_task }}': {{ fe_pure_storage_fa_vol__rename_tag_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "pure_storage/fa_vol__rename_tag | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_pure_storage_fa_vol__rename_tag_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_pure_storage_fa_vol__rename_tag_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_pure_storage_fa_vol__rename_tag_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "pure_storage/fa_vol__rename_tag | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "pure_storage/fa_vol__rename_tag":
              component: "pure_storage/fa_vol__rename_tag"
              type: "tasks"
              platform: "pure_storage"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_pure_storage_fa_vol__rename_tag_status | default('unknown') }}"
              preflight_passed: "{{ fe_pure_storage_fa_vol__rename_tag_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_pure_storage_fa_vol__rename_tag_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_pure_storage_fa_vol__rename_tag_started_at | default('') }}"
              ended_at: "{{ fe_pure_storage_fa_vol__rename_tag_ended_at | default('') }}"
              duration_seconds: "{{ fe_pure_storage_fa_vol__rename_tag_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_pure_storage_fa_vol__rename_tag_failed_task | default('') }}"
              error: "{{ fe_pure_storage_fa_vol__rename_tag_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_pure_storage_fa_vol__rename_tag_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_pure_storage_fa_vol__rename_tag_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_pure_storage_fa_vol__rename_tag_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_pure_storage_fa_vol__rename_tag_status | default('') == 'succeeded' else ['pure_storage/fa_vol__rename_tag'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "pure_storage/fa_vol__rename_tag | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_pure_storage_fa_vol__rename_tag_stats_published: true
      tags:
        - always
