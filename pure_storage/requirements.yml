---
# Pure Storage FlashArray Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: purestorage.flasharray
    version: ">=1.24.0"

  - name: community.general
    version: ">=8.0.0"

# Python dependencies:
# pip install purestorage py-pure-client
