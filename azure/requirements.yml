---
# Azure Ansible Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: azure.azcollection
    version: ">=2.1.0"

  - name: community.general
    version: ">=8.0.0"

  - name: ansible.posix
    version: ">=1.5.0"

# Python dependencies (install separately):
# pip install azure-cli azure-identity azure-mgmt-resource azure-mgmt-compute
# pip install azure-mgmt-network azure-mgmt-storage azure-mgmt-keyvault
# pip install azure-mgmt-containerservice azure-mgmt-sql azure-mgmt-monitor
