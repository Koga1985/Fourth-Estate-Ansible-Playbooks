{{ ansible_managed | comment }}
# Application Security & Development STIG — Evidence

- **Application:** {{ appsec_evidence.meta.application }}
- **Source:** {{ appsec_evidence.meta.source }}
- **Benchmark:** {{ stig_benchmark }} {{ stig_version }}
- **Generated:** {{ appsec_evidence.meta.timestamp }}
- **Gate mode:** {{ appsec_evidence.meta.gate }}
- **Frameworks:** {{ compliance_frameworks | join(', ') }}

## Scanner findings

| Scanner | Available | Findings | Critical | High |
|---------|-----------|----------|----------|------|
{% for tool in ['secrets','sast','dependencies','iac'] %}
| {{ tool }} | {{ 'yes' if appsec_available[tool] | default(false) else 'no (manual evidence required)' }} | {{ appsec_findings[tool].count | default('-') }} | {{ appsec_findings[tool].critical | default('-') }} | {{ appsec_findings[tool].high | default('-') }} |
{% endfor %}

**Totals:** critical={{ appsec_evidence.totals.critical }}, high={{ appsec_evidence.totals.high }}, secrets={{ appsec_evidence.totals.secrets }}

## APSC-DV control evaluation

| APSC-DV ID | Control | Evidence | Result |
|------------|---------|----------|--------|
{% for c in appsec_evidence.controls %}
| {{ c.id }} | {{ c.title }} | {{ c.evidence | default('manual') }} | {{ c.result }} |
{% endfor %}

> `pass` = scanner ran with zero findings · `findings` = scanner reported issues to
> remediate · `tool-not-available` = install the scanner or attach manual evidence ·
> `manual-review` = procedural control requiring documented verification.

## Recommended scanners

| Evidence | Tool | Install |
|----------|------|---------|
| secrets | gitleaks | `https://github.com/gitleaks/gitleaks` |
| sast | semgrep | `pip install semgrep` |
| dependencies | grype | `https://github.com/anchore/grype` |
| iac | checkov | `pip install checkov` |
