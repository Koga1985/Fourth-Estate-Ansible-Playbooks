---

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['dragos/dragos_flows__ngfw_template_checkpoint']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_dragos_dragos_flows__ngfw_template_checkpoint_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "dragos/dragos_flows__ngfw_template_checkpoint | Initialize validation state"
  ansible.builtin.set_fact:
    fe_dragos_dragos_flows__ngfw_template_checkpoint_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_dragos_dragos_flows__ngfw_template_checkpoint_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_dragos_dragos_flows__ngfw_template_checkpoint_status: "running"
    fe_dragos_dragos_flows__ngfw_template_checkpoint_preflight_passed: false
    fe_dragos_dragos_flows__ngfw_template_checkpoint_postflight_passed: false
    fe_dragos_dragos_flows__ngfw_template_checkpoint_failed_task: ""
    fe_dragos_dragos_flows__ngfw_template_checkpoint_error: ""
    fe_dragos_dragos_flows__ngfw_template_checkpoint_stats_published: false
  tags:
    - always

- name: "dragos/dragos_flows__ngfw_template_checkpoint | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          dragos/dragos_flows__ngfw_template_checkpoint: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          dragos/dragos_flows__ngfw_template_checkpoint: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "dragos/dragos_flows__ngfw_template_checkpoint: required variable '{{ item }}' is not defined."
        success_msg: "dragos/dragos_flows__ngfw_template_checkpoint: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_dragos_dragos_flows__ngfw_template_checkpoint_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Load allowlist
      ansible.builtin.set_fact:
        allowlist: "{{ lookup('ansible.builtin.file', artifacts_dir | default('/tmp/dragos-artifacts') + '/allowlist.json') | from_json }}"
    - name: Render Check Point rules CSV
      ansible.builtin.copy:
        dest: "{{ artifacts_dir | default('/tmp/dragos-artifacts') }}/checkpoint_rules.csv"
        mode: "0644"
        content: |
          name,source,destination,service,action,track,install-on
          {% for r in allowlist %}
          ALLOW_{{ loop.index }},{{ r.src }},{{ r.dst }},{{ r.service }},Accept,Log,Policy Targets
          {% endfor %}

    # ----------------------------- postflight -------------------

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_dragos_dragos_flows__ngfw_template_checkpoint_preflight_passed | bool
          - fe_dragos_dragos_flows__ngfw_template_checkpoint_status in ['running', 'succeeded']
        fail_msg: >-
          dragos/dragos_flows__ngfw_template_checkpoint: postflight validation failed - preflight passed
          ={{ fe_dragos_dragos_flows__ngfw_template_checkpoint_preflight_passed | default(false) }},
          status={{ fe_dragos_dragos_flows__ngfw_template_checkpoint_status | default('unknown') }}.
        success_msg: "dragos/dragos_flows__ngfw_template_checkpoint: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_dragos_dragos_flows__ngfw_template_checkpoint_postflight_passed: true
        fe_dragos_dragos_flows__ngfw_template_checkpoint_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_dragos_dragos_flows__ngfw_template_checkpoint_status: "failed"
        fe_dragos_dragos_flows__ngfw_template_checkpoint_postflight_passed: false
        fe_dragos_dragos_flows__ngfw_template_checkpoint_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_dragos_dragos_flows__ngfw_template_checkpoint_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          dragos/dragos_flows__ngfw_template_checkpoint: FAILED at '{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_dragos_dragos_flows__ngfw_template_checkpoint_error }}
      tags:
        - always
        - rescue

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          dragos/dragos_flows__ngfw_template_checkpoint failed at '{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_failed_task }}': {{ fe_dragos_dragos_flows__ngfw_template_checkpoint_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_dragos_dragos_flows__ngfw_template_checkpoint_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_dragos_dragos_flows__ngfw_template_checkpoint_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_dragos_dragos_flows__ngfw_template_checkpoint_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "dragos/dragos_flows__ngfw_template_checkpoint":
              component: "dragos/dragos_flows__ngfw_template_checkpoint"
              type: "tasks"
              platform: "dragos"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_status | default('unknown') }}"
              preflight_passed: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_started_at | default('') }}"
              ended_at: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_ended_at | default('') }}"
              duration_seconds: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_failed_task | default('') }}"
              error: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_dragos_dragos_flows__ngfw_template_checkpoint_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_dragos_dragos_flows__ngfw_template_checkpoint_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_dragos_dragos_flows__ngfw_template_checkpoint_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_dragos_dragos_flows__ngfw_template_checkpoint_status | default('') == 'succeeded' else ['dragos/dragos_flows__ngfw_template_checkpoint'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "dragos/dragos_flows__ngfw_template_checkpoint | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_dragos_dragos_flows__ngfw_template_checkpoint_stats_published: true
      tags:
        - always
