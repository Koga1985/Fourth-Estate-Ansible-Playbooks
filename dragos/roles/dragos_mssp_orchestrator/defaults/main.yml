---
# Role defaults

dragos_base_url: "https://tenant.dragos.com"
dragos_token: "{{ lookup('env','DRAGOS_TOKEN') }}"
dragos_verify_ssl: true
artifacts_dir: "/tmp/dragos-artifacts"
page_size: 500
dry_run: true

tenants: []              # [ { name: plant1, base_url: "https://...", token: "..." } ]
action_task: ""          # e.g., "roles/dragos_alerts_pipeline/tasks/pull_filtered.yml"
action_vars: {}        # optional extra vars to pass to the included task
