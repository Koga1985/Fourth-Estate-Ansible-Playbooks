
---
- name: Bundle CSVs
  ansible.builtin.archive:
    path:
      - "{{ artifacts_dir }}/alerts_filtered.csv"
      - "{{ artifacts_dir }}/audit_events.csv"
      - "{{ artifacts_dir }}/rbac_violations.csv"
    dest: "{{ artifacts_dir }}/compliance_pack.zip"
    format: zip
