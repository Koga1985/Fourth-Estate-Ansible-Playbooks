<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Dragos Sensor Operations Report - Fourth Estate</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; background-color: #f5f5f5; }
        .header { background-color: #003366; color: white; padding: 20px; text-align: center; }
        .classification { background-color: #ffd700; color: #000; padding: 10px; text-align: center; font-weight: bold; }
        .summary { background-color: white; padding: 20px; margin: 20px 0; border-radius: 5px; }
        .sensor-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; margin: 20px 0; }
        .sensor-card { background-color: white; padding: 15px; border-radius: 5px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        .sensor-card h3 { color: #003366; margin-top: 0; }
        .sensor-card .status { font-size: 18px; font-weight: bold; margin: 10px 0; }
        .status-healthy { color: #28a745; }
        .status-unhealthy { color: #dc3545; }
    </style>
</head>
<body>
    <div class="classification">UNCLASSIFIED // FOR OFFICIAL USE ONLY</div>

    <div class="header">
        <h1>Dragos Sensor Operations Report</h1>
        <h2>Fourth Estate OT/ICS Threat Detection</h2>
        <p>Generated: {{ ansible_date_time.iso8601 }}</p>
    </div>

    <div class="summary">
        <h2>Operations Summary</h2>
        <p><strong>Total Sensors:</strong> {{ sensor_summary.total_sensors }}</p>
        <p><strong>Healthy Sensors:</strong> <span class="status-healthy">{{ sensor_summary.healthy_sensors }}</span></p>
        <p><strong>Unhealthy Sensors:</strong> <span class="status-unhealthy">{{ sensor_summary.unhealthy_sensors }}</span></p>
        <p><strong>Virtual Sensors:</strong> {{ sensor_summary.virtual_sensors }}</p>
        <p><strong>Physical Sensors:</strong> {{ sensor_summary.physical_sensors }}</p>
    </div>

    <h2>Sensor Inventory</h2>
    <div class="sensor-grid">
    {% set items = (complete_sensor_inventory.json.items if complete_sensor_inventory.json.items is defined else complete_sensor_inventory.json.data | default([])) %}
    {% for sensor in items %}
        <div class="sensor-card">
            <h3>{{ sensor.name }}</h3>
            <div class="status status-{{ sensor.status }}">{{ sensor.status | upper }}</div>
            <p><strong>Type:</strong> {{ sensor.type | default('N/A') }}</p>
            <p><strong>Version:</strong> {{ sensor.version | default('N/A') }}</p>
            <p><strong>Site:</strong> {{ sensor.site | default('N/A') }}</p>
            <p><strong>Zone:</strong> {{ sensor.zone | default('N/A') }}</p>
        </div>
    {% endfor %}
    </div>

    <div class="classification">UNCLASSIFIED // FOR OFFICIAL USE ONLY</div>
</body>
</html>
