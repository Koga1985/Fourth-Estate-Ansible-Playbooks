---

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['ansible/ans_perf_scaling']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_ansible_ans_perf_scaling_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "ansible/ans_perf_scaling | Initialize validation state"
  ansible.builtin.set_fact:
    fe_ansible_ans_perf_scaling_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_ansible_ans_perf_scaling_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_ansible_ans_perf_scaling_status: "running"
    fe_ansible_ans_perf_scaling_preflight_passed: false
    fe_ansible_ans_perf_scaling_postflight_passed: false
    fe_ansible_ans_perf_scaling_failed_task: ""
    fe_ansible_ans_perf_scaling_error: ""
    fe_ansible_ans_perf_scaling_stats_published: false
  tags:
    - always

- name: "ansible/ans_perf_scaling | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "ansible/ans_perf_scaling | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          ansible/ans_perf_scaling: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          ansible/ans_perf_scaling: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "ansible/ans_perf_scaling | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "ansible/ans_perf_scaling: required variable '{{ item }}' is not defined."
        success_msg: "ansible/ans_perf_scaling: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_ansible_ans_perf_scaling_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "ansible/ans_perf_scaling | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_ansible_ans_perf_scaling_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Enable fact caching
      when: apply_changes | bool and enable_fact_caching | bool
      ansible.controller.settings:
        controller_host: "{{ controller_host }}"
        controller_oauthtoken: "{{ controller_oauthtoken }}"
        validate_certs: "{{ validate_certs }}"
        settings:
          AWX_FACTS_CACHE_TIMEOUT: "{{ fact_cache_timeout }}"

    - name: Configure database pool
      when: apply_changes | bool
      ansible.controller.settings:
        controller_host: "{{ controller_host }}"
        controller_oauthtoken: "{{ controller_oauthtoken }}"
        validate_certs: "{{ validate_certs }}"
        settings:
          DATABASE_POOL_SIZE: "{{ db_connection_pool_size }}"
          DATABASE_MAX_OVERFLOW: "{{ db_max_overflow }}"

    - name: Enable pipelining
      when: apply_changes | bool and enable_pipelining | bool
      ansible.controller.settings:
        controller_host: "{{ controller_host }}"
        controller_oauthtoken: "{{ controller_oauthtoken }}"
        validate_certs: "{{ validate_certs }}"
        settings:
          AWX_PIPELINING: true

    - name: Display performance summary
      ansible.builtin.debug:
        msg:
          - "Fact Caching: {{ enable_fact_caching }}"
          - "Max Forks: {{ max_forks }}"
          - "HA Enabled: {{ fourth_estate_high_availability }}"

    # ----------------------------- postflight -------------------

    - name: "ansible/ans_perf_scaling | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_ansible_ans_perf_scaling_preflight_passed | bool
          - fe_ansible_ans_perf_scaling_status in ['running', 'succeeded']
        fail_msg: >-
          ansible/ans_perf_scaling: postflight validation failed - preflight passed
          ={{ fe_ansible_ans_perf_scaling_preflight_passed | default(false) }},
          status={{ fe_ansible_ans_perf_scaling_status | default('unknown') }}.
        success_msg: "ansible/ans_perf_scaling: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "ansible/ans_perf_scaling | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_ansible_ans_perf_scaling_postflight_passed: true
        fe_ansible_ans_perf_scaling_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "ansible/ans_perf_scaling | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_ansible_ans_perf_scaling_status: "failed"
        fe_ansible_ans_perf_scaling_postflight_passed: false
        fe_ansible_ans_perf_scaling_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_ansible_ans_perf_scaling_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "ansible/ans_perf_scaling | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          ansible/ans_perf_scaling: FAILED at '{{ fe_ansible_ans_perf_scaling_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_ansible_ans_perf_scaling_error }}
      tags:
        - always
        - rescue

    - name: "ansible/ans_perf_scaling | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          ansible/ans_perf_scaling failed at '{{ fe_ansible_ans_perf_scaling_failed_task }}': {{ fe_ansible_ans_perf_scaling_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "ansible/ans_perf_scaling | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_ansible_ans_perf_scaling_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_ansible_ans_perf_scaling_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_ansible_ans_perf_scaling_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "ansible/ans_perf_scaling | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "ansible/ans_perf_scaling":
              component: "ansible/ans_perf_scaling"
              type: "role"
              platform: "ansible"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_ansible_ans_perf_scaling_status | default('unknown') }}"
              preflight_passed: "{{ fe_ansible_ans_perf_scaling_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_ansible_ans_perf_scaling_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_ansible_ans_perf_scaling_started_at | default('') }}"
              ended_at: "{{ fe_ansible_ans_perf_scaling_ended_at | default('') }}"
              duration_seconds: "{{ fe_ansible_ans_perf_scaling_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_ansible_ans_perf_scaling_failed_task | default('') }}"
              error: "{{ fe_ansible_ans_perf_scaling_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_ansible_ans_perf_scaling_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_ansible_ans_perf_scaling_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_ansible_ans_perf_scaling_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_ansible_ans_perf_scaling_status | default('') == 'succeeded' else ['ansible/ans_perf_scaling'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "ansible/ans_perf_scaling | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_ansible_ans_perf_scaling_stats_published: true
      tags:
        - always
