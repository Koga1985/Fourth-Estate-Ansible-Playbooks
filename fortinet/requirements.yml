---
# Fortinet Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: fortinet.fortios
    version: ">=2.3.0"

  - name: fortinet.fortimanager
    version: ">=2.4.0"

  - name: community.general
    version: ">=8.0.0"

# Python dependencies:
# pip install fortiosapi
