---
# Grab-and-go entry point for the MySQL 8.0 STIG role.
# Dry-run: ansible-playbook -i inventory.example run.yml -e @vars.example.yml
# Enforce: ansible-playbook -i inventory.example run.yml -e @vars.example.yml -e apply_changes=true
- name: Apply Oracle MySQL 8.0 STIG hardening
  hosts: mysql_servers
  gather_facts: true
  any_errors_fatal: false
  collections:
    - community.mysql
  roles:
    - role: mysql80_stig
