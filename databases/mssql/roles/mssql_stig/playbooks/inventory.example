# Example inventory for the SQL Server STIG role.
# Runs from any host that can reach the SQL Server instance (needs pymssql).
[mssql_servers]
mssql-01 ansible_host=10.0.0.98

[mssql_servers:vars]
ansible_connection=local
