# Managed by Ansible (postgresql_backup). PostgreSQL password file.
# format: hostname:port:database:username:password
{{ postgresql_host | default('localhost') }}:{{ postgresql_port | default(5432) }}:*:{{ postgresql_superuser | default('postgres') }}:{{ postgresql_superuser_password | default('') }}
