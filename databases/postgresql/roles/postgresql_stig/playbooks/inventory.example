# Example inventory for the PostgreSQL STIG role.
# Run on the DB host (local socket) or set pg_login_host to a remote server.
[postgresql_servers]
pg-01 ansible_host=10.0.0.95

[postgresql_servers:vars]
ansible_user=ansible
ansible_become=true
