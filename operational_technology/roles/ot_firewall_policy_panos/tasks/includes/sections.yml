---
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir | default('/tmp/ot-artifacts') }}"
    state: directory
    mode: "0755"

- name: Create sections (panos)
  ansible.builtin.debug: { msg: "Sections for panos would be applied here (API calls by vars)." }
