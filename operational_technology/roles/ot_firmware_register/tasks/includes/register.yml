---
# OT firmware register workflow — NOT YET IMPLEMENTED (documented gap)
#
# This workflow is intentionally a fail-fast placeholder rather than unverified
# automation against OT/ICS firmware registries and devices, which would be unsafe to ship untested.
# See KNOWN_LIMITATIONS.md and PRODUCTION_READINESS_ASSESSMENT.md.
#
# The default path is unaffected: this only runs when ot_firmware_operation == 'register'.
- name: OT firmware register workflow is not implemented
  ansible.builtin.fail:
    msg: >-
      The 'register' workflow is not implemented in this role yet.
      Provide a site-specific implementation, or do not enable it (ot_firmware_operation == 'register'),
      before running this path in production.
