---
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir | default('/tmp/ot-artifacts') }}"
    state: directory
    mode: "0755"

- name: Apply tuning (capture filters, bandwidth caps) — placeholder
  ansible.builtin.copy:
    dest: "{{ artifacts_dir }}/sensors_tuning.json"
    mode: "0644"
    content: "{{ tuning | default({}) | to_nice_json }}"
