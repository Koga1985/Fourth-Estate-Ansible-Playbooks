---
- name: Converge
  hosts: all
  gather_facts: false
  vars:
    apply_changes: false
  tasks:
    - name: Run the ot_firewall_policy_checkpoint role
      ansible.builtin.include_role:
        name: ot_firewall_policy_checkpoint

    - name: Assert the validation harness reported success
      ansible.builtin.assert:
        that:
          - fe_operational_technology_ot_firewall_policy_checkpoint_preflight_passed | bool
          - fe_operational_technology_ot_firewall_policy_checkpoint_postflight_passed | bool
          - fe_operational_technology_ot_firewall_policy_checkpoint_status == 'succeeded'
        fail_msg: >-
          ot_firewall_policy_checkpoint did not complete cleanly:
          status={{ fe_operational_technology_ot_firewall_policy_checkpoint_status | default('unset') }}
          failed_task={{ fe_operational_technology_ot_firewall_policy_checkpoint_failed_task | default('') }}
        success_msg: "ot_firewall_policy_checkpoint completed and the validation harness reported success."
