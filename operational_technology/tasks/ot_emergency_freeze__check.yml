---

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['operational_technology/ot_emergency_freeze__check']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_operational_technology_ot_emergency_freeze__check_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "operational_technology/ot_emergency_freeze__check | Initialize validation state"
  ansible.builtin.set_fact:
    fe_operational_technology_ot_emergency_freeze__check_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_operational_technology_ot_emergency_freeze__check_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_operational_technology_ot_emergency_freeze__check_status: "running"
    fe_operational_technology_ot_emergency_freeze__check_preflight_passed: false
    fe_operational_technology_ot_emergency_freeze__check_postflight_passed: false
    fe_operational_technology_ot_emergency_freeze__check_failed_task: ""
    fe_operational_technology_ot_emergency_freeze__check_error: ""
    fe_operational_technology_ot_emergency_freeze__check_stats_published: false
  tags:
    - always

- name: "operational_technology/ot_emergency_freeze__check | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "operational_technology/ot_emergency_freeze__check | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          operational_technology/ot_emergency_freeze__check: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          operational_technology/ot_emergency_freeze__check: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "operational_technology/ot_emergency_freeze__check | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "operational_technology/ot_emergency_freeze__check: required variable '{{ item }}' is not defined."
        success_msg: "operational_technology/ot_emergency_freeze__check: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_operational_technology_ot_emergency_freeze__check_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "operational_technology/ot_emergency_freeze__check | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_operational_technology_ot_emergency_freeze__check_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Read freeze flag from file (optional)
      when: sot_freeze_file | default('') != ''
      ansible.builtin.slurp:
        src: "{{ sot_freeze_file }}"
      register: freeze_raw
    - name: Determine freeze status
      ansible.builtin.set_fact:
        freeze_active: >-
          {%- if freeze_raw is defined -%}
          {{ (freeze_raw.content | b64decode | trim) in ['1','true','True','TRUE','yes','on'] }}
          {%- else -%}
          {{ freeze_flag | default(false) }}
          {%- endif -%}
    - name: Hard stop if freeze is active
      ansible.builtin.assert:
        that: [ not (freeze_active | bool) ]
        fail_msg: "Emergency freeze active — blocking changes."

    # ----------------------------- postflight -------------------

    - name: "operational_technology/ot_emergency_freeze__check | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_operational_technology_ot_emergency_freeze__check_preflight_passed | bool
          - fe_operational_technology_ot_emergency_freeze__check_status in ['running', 'succeeded']
        fail_msg: >-
          operational_technology/ot_emergency_freeze__check: postflight validation failed - preflight passed
          ={{ fe_operational_technology_ot_emergency_freeze__check_preflight_passed | default(false) }},
          status={{ fe_operational_technology_ot_emergency_freeze__check_status | default('unknown') }}.
        success_msg: "operational_technology/ot_emergency_freeze__check: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "operational_technology/ot_emergency_freeze__check | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_operational_technology_ot_emergency_freeze__check_postflight_passed: true
        fe_operational_technology_ot_emergency_freeze__check_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "operational_technology/ot_emergency_freeze__check | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_operational_technology_ot_emergency_freeze__check_status: "failed"
        fe_operational_technology_ot_emergency_freeze__check_postflight_passed: false
        fe_operational_technology_ot_emergency_freeze__check_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_operational_technology_ot_emergency_freeze__check_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "operational_technology/ot_emergency_freeze__check | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          operational_technology/ot_emergency_freeze__check: FAILED at '{{ fe_operational_technology_ot_emergency_freeze__check_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_operational_technology_ot_emergency_freeze__check_error }}
      tags:
        - always
        - rescue

    - name: "operational_technology/ot_emergency_freeze__check | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          operational_technology/ot_emergency_freeze__check failed at '{{ fe_operational_technology_ot_emergency_freeze__check_failed_task }}': {{ fe_operational_technology_ot_emergency_freeze__check_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "operational_technology/ot_emergency_freeze__check | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_operational_technology_ot_emergency_freeze__check_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_operational_technology_ot_emergency_freeze__check_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_operational_technology_ot_emergency_freeze__check_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "operational_technology/ot_emergency_freeze__check | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "operational_technology/ot_emergency_freeze__check":
              component: "operational_technology/ot_emergency_freeze__check"
              type: "tasks"
              platform: "operational_technology"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_operational_technology_ot_emergency_freeze__check_status | default('unknown') }}"
              preflight_passed: "{{ fe_operational_technology_ot_emergency_freeze__check_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_operational_technology_ot_emergency_freeze__check_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_operational_technology_ot_emergency_freeze__check_started_at | default('') }}"
              ended_at: "{{ fe_operational_technology_ot_emergency_freeze__check_ended_at | default('') }}"
              duration_seconds: "{{ fe_operational_technology_ot_emergency_freeze__check_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_operational_technology_ot_emergency_freeze__check_failed_task | default('') }}"
              error: "{{ fe_operational_technology_ot_emergency_freeze__check_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_operational_technology_ot_emergency_freeze__check_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_operational_technology_ot_emergency_freeze__check_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_operational_technology_ot_emergency_freeze__check_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_operational_technology_ot_emergency_freeze__check_status | default('') == 'succeeded' else ['operational_technology/ot_emergency_freeze__check'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "operational_technology/ot_emergency_freeze__check | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_operational_technology_ot_emergency_freeze__check_stats_published: true
      tags:
        - always
