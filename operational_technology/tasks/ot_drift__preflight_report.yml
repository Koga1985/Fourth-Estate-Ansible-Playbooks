---
# Compare live vs desired for selected domains.
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir | default('/tmp/ot-artifacts') }}"
    state: directory
    mode: "0755"

- name: Write preflight drift report (placeholder)
  ansible.builtin.copy:
    dest: "{{ artifacts_dir }}/preflight_drift.txt"
    mode: "0644"
    content: |
      Compare live vs desired across selected objects (operators, CRDs, policies) — implement per domain.
