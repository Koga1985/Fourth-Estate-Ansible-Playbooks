---
# Palo Alto Networks Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: paloaltonetworks.panos
    version: ">=2.17.0"

  - name: community.general
    version: ">=8.0.0"

# Python dependencies:
# pip install pan-python pandevice xmltodict
