---
# handlers file for elasticsearch_security

- name: restart elasticsearch
  ansible.builtin.systemd:
    name: elasticsearch
    state: restarted
  when: elasticsearch_service_state | default('started') == 'started'
