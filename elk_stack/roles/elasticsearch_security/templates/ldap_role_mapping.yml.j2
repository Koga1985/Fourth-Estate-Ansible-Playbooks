# Managed by Ansible (elasticsearch_security) - LDAP role mappings
# Maps Elasticsearch roles to LDAP group DNs.
{% for role, groups in (elasticsearch_ldap_role_mappings | default({})).items() %}
{{ role }}:
{% for g in groups %}
  - "{{ g }}"
{% endfor %}
{% endfor %}
