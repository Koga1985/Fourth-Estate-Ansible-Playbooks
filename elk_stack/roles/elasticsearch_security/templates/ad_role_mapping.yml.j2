# Managed by Ansible (elasticsearch_security) - Active Directory role mappings
# Maps Elasticsearch roles to AD group DNs.
{% for role, groups in (elasticsearch_ad_role_mappings | default({})).items() %}
{{ role }}:
{% for g in groups %}
  - "{{ g }}"
{% endfor %}
{% endfor %}
