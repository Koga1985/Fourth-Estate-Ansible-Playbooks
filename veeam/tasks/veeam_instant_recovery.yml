---
# Inputs: source_vm_id, restore_point_id, target_vm_name, target_datastore_id, target_network_id

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['veeam/veeam_instant_recovery']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_veeam_veeam_instant_recovery_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "veeam/veeam_instant_recovery | Initialize validation state"
  ansible.builtin.set_fact:
    fe_veeam_veeam_instant_recovery_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_veeam_veeam_instant_recovery_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_veeam_veeam_instant_recovery_status: "running"
    fe_veeam_veeam_instant_recovery_preflight_passed: false
    fe_veeam_veeam_instant_recovery_postflight_passed: false
    fe_veeam_veeam_instant_recovery_failed_task: ""
    fe_veeam_veeam_instant_recovery_error: ""
    fe_veeam_veeam_instant_recovery_stats_published: false
  tags:
    - always

- name: "veeam/veeam_instant_recovery | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "veeam/veeam_instant_recovery | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          veeam/veeam_instant_recovery: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          veeam/veeam_instant_recovery: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "veeam/veeam_instant_recovery | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "veeam/veeam_instant_recovery: required variable '{{ item }}' is not defined."
        success_msg: "veeam/veeam_instant_recovery: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_veeam_veeam_instant_recovery_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "veeam/veeam_instant_recovery | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_veeam_veeam_instant_recovery_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Start Instant Recovery
      ansible.builtin.uri:
        url: "{{ veeam_base_url }}/api/v1/restoreSessions/instantVmRecovery"
        method: POST
        headers:
          Authorization: "Bearer {{ veeam_token }}"
          Content-Type: "application/json"
        body_format: json
        body:
          sourceVmObjectId: "{{ source_vm_id }}"
          restorePointId: "{{ restore_point_id }}"
          targetVmName: "{{ target_vm_name }}"
          powerOn: true
          network:
            type: "Custom"
            targetNetworkId: "{{ target_network_id }}"
          datastoreId: "{{ target_datastore_id }}"
        status_code: [200,202]
        validate_certs: "{{ veeam_verify_tls }}"
      register: ir

    # ----------------------------- postflight -------------------

    - name: "veeam/veeam_instant_recovery | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_veeam_veeam_instant_recovery_preflight_passed | bool
          - fe_veeam_veeam_instant_recovery_status in ['running', 'succeeded']
        fail_msg: >-
          veeam/veeam_instant_recovery: postflight validation failed - preflight passed
          ={{ fe_veeam_veeam_instant_recovery_preflight_passed | default(false) }},
          status={{ fe_veeam_veeam_instant_recovery_status | default('unknown') }}.
        success_msg: "veeam/veeam_instant_recovery: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "veeam/veeam_instant_recovery | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_veeam_veeam_instant_recovery_postflight_passed: true
        fe_veeam_veeam_instant_recovery_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "veeam/veeam_instant_recovery | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_veeam_veeam_instant_recovery_status: "failed"
        fe_veeam_veeam_instant_recovery_postflight_passed: false
        fe_veeam_veeam_instant_recovery_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_veeam_veeam_instant_recovery_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "veeam/veeam_instant_recovery | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          veeam/veeam_instant_recovery: FAILED at '{{ fe_veeam_veeam_instant_recovery_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_veeam_veeam_instant_recovery_error }}
      tags:
        - always
        - rescue

    - name: "veeam/veeam_instant_recovery | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          veeam/veeam_instant_recovery failed at '{{ fe_veeam_veeam_instant_recovery_failed_task }}': {{ fe_veeam_veeam_instant_recovery_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "veeam/veeam_instant_recovery | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_veeam_veeam_instant_recovery_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_veeam_veeam_instant_recovery_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_veeam_veeam_instant_recovery_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "veeam/veeam_instant_recovery | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "veeam/veeam_instant_recovery":
              component: "veeam/veeam_instant_recovery"
              type: "tasks"
              platform: "veeam"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_veeam_veeam_instant_recovery_status | default('unknown') }}"
              preflight_passed: "{{ fe_veeam_veeam_instant_recovery_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_veeam_veeam_instant_recovery_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_veeam_veeam_instant_recovery_started_at | default('') }}"
              ended_at: "{{ fe_veeam_veeam_instant_recovery_ended_at | default('') }}"
              duration_seconds: "{{ fe_veeam_veeam_instant_recovery_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_veeam_veeam_instant_recovery_failed_task | default('') }}"
              error: "{{ fe_veeam_veeam_instant_recovery_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_veeam_veeam_instant_recovery_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_veeam_veeam_instant_recovery_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_veeam_veeam_instant_recovery_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_veeam_veeam_instant_recovery_status | default('') == 'succeeded' else ['veeam/veeam_instant_recovery'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "veeam/veeam_instant_recovery | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_veeam_veeam_instant_recovery_stats_published: true
      tags:
        - always
