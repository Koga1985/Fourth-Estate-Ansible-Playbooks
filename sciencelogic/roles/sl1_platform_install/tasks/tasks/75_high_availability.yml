---
# SL1 high availability setup — NOT YET IMPLEMENTED (documented gap)
#
# This workflow is intentionally a fail-fast placeholder rather than unverified
# automation against SL1 HA pairs, which would be unsafe to ship untested.
# See KNOWN_LIMITATIONS.md and PRODUCTION_READINESS_ASSESSMENT.md.
#
# The default path is unaffected: this only runs when sl1_ha.enabled is true.
- name: SL1 high availability setup is not implemented
  ansible.builtin.fail:
    msg: >-
      The 'high_availability' workflow is not implemented in this role yet.
      Provide a site-specific implementation, or do not enable it (sl1_ha.enabled is true),
      before running this path in production.
