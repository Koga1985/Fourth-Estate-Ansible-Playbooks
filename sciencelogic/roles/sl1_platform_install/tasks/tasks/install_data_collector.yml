---
# SL1 data collector install — NOT YET IMPLEMENTED (documented gap)
#
# This workflow is intentionally a fail-fast placeholder rather than unverified
# automation against SL1 data collector appliances, which would be unsafe to ship untested.
# See KNOWN_LIMITATIONS.md and PRODUCTION_READINESS_ASSESSMENT.md.
#
# The default path is unaffected: this only runs when a data collector has install=true.
- name: SL1 data collector install is not implemented
  ansible.builtin.fail:
    msg: >-
      The 'data_collector' workflow is not implemented in this role yet.
      Provide a site-specific implementation, or do not enable it (a data collector has install=true),
      before running this path in production.
