---
- name: Install Data Collectors
  ansible.builtin.include_tasks: install_data_collector.yml
  loop: "{{ sl1_data_collectors | selectattr('install', 'equalto', true) | list }}"
  loop_control:
    loop_var: collector
