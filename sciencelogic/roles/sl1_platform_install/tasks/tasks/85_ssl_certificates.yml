---

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['sciencelogic/sl1_platform_install/85_ssl_certificates']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_sciencelogic_sl1_platform_install_85_ssl_certificates_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Initialize validation state"
  ansible.builtin.set_fact:
    fe_sciencelogic_sl1_platform_install_85_ssl_certificates_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_sciencelogic_sl1_platform_install_85_ssl_certificates_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_sciencelogic_sl1_platform_install_85_ssl_certificates_status: "running"
    fe_sciencelogic_sl1_platform_install_85_ssl_certificates_preflight_passed: false
    fe_sciencelogic_sl1_platform_install_85_ssl_certificates_postflight_passed: false
    fe_sciencelogic_sl1_platform_install_85_ssl_certificates_failed_task: ""
    fe_sciencelogic_sl1_platform_install_85_ssl_certificates_error: ""
    fe_sciencelogic_sl1_platform_install_85_ssl_certificates_stats_published: false
  tags:
    - always

- name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          sciencelogic/sl1_platform_install/85_ssl_certificates: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          sciencelogic/sl1_platform_install/85_ssl_certificates: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "sciencelogic/sl1_platform_install/85_ssl_certificates: required variable '{{ item }}' is not defined."
        success_msg: "sciencelogic/sl1_platform_install/85_ssl_certificates: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Copy SSL certificate
      ansible.builtin.copy:
        src: "{{ sl1_ssl.certificate_path }}"
        dest: /opt/em7/conf/ssl/em7.crt
        owner: em7admin
        group: em7admin
        mode: '0644'
      when: not dry_run
      notify: Restart SL1 services

    - name: Copy SSL private key
      ansible.builtin.copy:
        src: "{{ sl1_ssl.private_key_path }}"
        dest: /opt/em7/conf/ssl/em7.key
        owner: em7admin
        group: em7admin
        mode: '0600'
      when: not dry_run
      notify: Restart SL1 services

    - name: Copy CA bundle
      ansible.builtin.copy:
        src: "{{ sl1_ssl.ca_bundle_path }}"
        dest: /opt/em7/conf/ssl/ca-bundle.crt
        owner: em7admin
        group: em7admin
        mode: '0644'
      when:
        - sl1_ssl.ca_bundle_path is defined
        - not dry_run
      notify: Restart SL1 services

    - name: Configure TLS version
      ansible.builtin.lineinfile:
        path: /opt/em7/conf/em7.conf
        regexp: '^ssl_protocols'
        line: "ssl_protocols = TLSv{{ sl1_ssl.tls_version }}"
      when: not dry_run
      notify: Restart SL1 services

    # ----------------------------- postflight -------------------

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_sciencelogic_sl1_platform_install_85_ssl_certificates_preflight_passed | bool
          - fe_sciencelogic_sl1_platform_install_85_ssl_certificates_status in ['running', 'succeeded']
        fail_msg: >-
          sciencelogic/sl1_platform_install/85_ssl_certificates: postflight validation failed - preflight passed
          ={{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_preflight_passed | default(false) }},
          status={{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_status | default('unknown') }}.
        success_msg: "sciencelogic/sl1_platform_install/85_ssl_certificates: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_postflight_passed: true
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_status: "failed"
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_postflight_passed: false
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          sciencelogic/sl1_platform_install/85_ssl_certificates: FAILED at '{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_error }}
      tags:
        - always
        - rescue

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          sciencelogic/sl1_platform_install/85_ssl_certificates failed at '{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_failed_task }}': {{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_sciencelogic_sl1_platform_install_85_ssl_certificates_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "sciencelogic/sl1_platform_install/85_ssl_certificates":
              component: "sciencelogic/sl1_platform_install/85_ssl_certificates"
              type: "role-tasks"
              platform: "sciencelogic"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_status | default('unknown') }}"
              preflight_passed: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_started_at | default('') }}"
              ended_at: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_ended_at | default('') }}"
              duration_seconds: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_failed_task | default('') }}"
              error: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_sciencelogic_sl1_platform_install_85_ssl_certificates_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_sciencelogic_sl1_platform_install_85_ssl_certificates_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_sciencelogic_sl1_platform_install_85_ssl_certificates_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_sciencelogic_sl1_platform_install_85_ssl_certificates_status | default('') == 'succeeded' else ['sciencelogic/sl1_platform_install/85_ssl_certificates'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "sciencelogic/sl1_platform_install/85_ssl_certificates | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_platform_install_85_ssl_certificates_stats_published: true
      tags:
        - always
