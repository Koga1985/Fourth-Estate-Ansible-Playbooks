---
- name: Install Message Collectors
  ansible.builtin.include_tasks: install_message_collector.yml
  loop: "{{ sl1_message_collectors | selectattr('install', 'equalto', true) | list }}"
  loop_control:
    loop_var: collector
