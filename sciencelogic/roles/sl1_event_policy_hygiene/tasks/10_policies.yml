---
- name: Event policies plan
  ansible.builtin.copy:
    dest: "{{ artifacts_dir }}/event_policy_hygiene_plan.json"
    mode: "0644"
    content: "{{ {'suppressions': suppressions, 'autoclear': autoclear, 'dedupe': dedupe, 'quarantine': quarantine_policies} | to_nice_json }}"
