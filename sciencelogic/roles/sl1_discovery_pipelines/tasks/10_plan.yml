---
- name: Discovery pipeline plan
  ansible.builtin.copy:
    dest: "{{ artifacts_dir }}/discovery_pipeline_plan.json"
    mode: "0644"
    content: "{{ {'ranges': ranges, 'device_classes': device_classes, 'credentials': credentials, 'pipeline_rules': pipeline_rules} | to_nice_json }}"
