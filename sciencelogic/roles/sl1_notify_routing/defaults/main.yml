---
sl1:
  url: "https://sl1.example.com"
  token: ""
  verify_ssl: true
artifacts_dir: "/tmp/sl1-artifacts"
dry_run: true

routes: []        # [{org, severity, channel: email/slack/teams/webhook, target, bhours, escalate_to, delay_min}]
webhooks: []      # optional definitions
