---
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir }}"
    state: directory
    mode: "0755"

- import_tasks: 10_admin_actions.yml
- import_tasks: 20_change_logs.yml
- import_tasks: 30_access_reviews.yml
