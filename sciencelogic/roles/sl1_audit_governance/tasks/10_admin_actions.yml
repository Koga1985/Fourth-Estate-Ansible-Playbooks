---

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['sciencelogic/sl1_audit_governance/10_admin_actions']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_sciencelogic_sl1_audit_governance_10_admin_actions_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "sciencelogic/sl1_audit_governance/10_admin_actions | Initialize validation state"
  ansible.builtin.set_fact:
    fe_sciencelogic_sl1_audit_governance_10_admin_actions_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_sciencelogic_sl1_audit_governance_10_admin_actions_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_sciencelogic_sl1_audit_governance_10_admin_actions_status: "running"
    fe_sciencelogic_sl1_audit_governance_10_admin_actions_preflight_passed: false
    fe_sciencelogic_sl1_audit_governance_10_admin_actions_postflight_passed: false
    fe_sciencelogic_sl1_audit_governance_10_admin_actions_failed_task: ""
    fe_sciencelogic_sl1_audit_governance_10_admin_actions_error: ""
    fe_sciencelogic_sl1_audit_governance_10_admin_actions_stats_published: false
  tags:
    - always

- name: "sciencelogic/sl1_audit_governance/10_admin_actions | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          sciencelogic/sl1_audit_governance/10_admin_actions: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          sciencelogic/sl1_audit_governance/10_admin_actions: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "sciencelogic/sl1_audit_governance/10_admin_actions: required variable '{{ item }}' is not defined."
        success_msg: "sciencelogic/sl1_audit_governance/10_admin_actions: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Admin actions export CSV
      ansible.builtin.copy:
        dest: "{{ artifacts_dir }}/audit_admin_actions.csv"
        mode: "0644"
        content: "time,user,action,target\n"

    # --------------------------- evidence delivery --------------------------
    # Writing the report to artifacts_dir is not delivery. Under Ansible
    # Automation Platform the playbook runs inside an execution-environment
    # container that is destroyed when the job ends, so a file left on that
    # filesystem is gone before anyone can read it. Republishing the report
    # through set_stats puts it in the job artifacts, which AAP persists and
    # passes to downstream workflow nodes.
    #
    # These reports carry compliance status and configuration posture, not
    # credentials. Job artifacts are visible to anyone with job-read
    # permission, which is wider than the file this supplements -- set
    # fe_evidence_publish=false to keep the file as the only copy.
    # See docs/VALIDATION_AND_STATS.md#compliance-evidence-delivery.

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Evidence | Locate the generated artifacts"
      ansible.builtin.find:
        paths: "{{ artifacts_dir }}"
        patterns: "audit_admin_actions.csv"
        file_type: file
      register: fe_evidence_found
      changed_when: false
      failed_when: false
      when: fe_evidence_publish | default(true) | bool
      tags: [report, artifact, evidence]

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Evidence | Read the artifacts within the size budget"
      ansible.builtin.slurp:
        src: "{{ item.path }}"
      register: fe_evidence_read
      loop: "{{ fe_evidence_found.files | default([]) | sort(attribute='path') }}"
      loop_control:
        label: "{{ item.path | basename }}"
      when:
        - fe_evidence_publish | default(true) | bool
        - item.size | int <= (fe_evidence_max_bytes | default(65536) | int)
      tags: [report, artifact, evidence]

    # An artifact too large to publish must not vanish silently: say so, so the
    # operator knows the job artifacts are not the complete record.
    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Evidence | Warn about artifacts that exceeded the size budget"
      ansible.builtin.debug:
        msg: >-
          Evidence artifact {{ item.path | basename }} is {{ item.size }} bytes,
          over the {{ fe_evidence_max_bytes | default(65536) }}-byte budget, and was NOT
          published to the job artifacts. It exists only at {{ item.path }}, which does
          not survive an execution-environment container. Raise fe_evidence_max_bytes
          or collect this file another way.
      loop: "{{ fe_evidence_found.files | default([]) | sort(attribute='path') }}"
      loop_control:
        label: "{{ item.path | basename }}"
      when:
        - fe_evidence_publish | default(true) | bool
        - item.size | int > (fe_evidence_max_bytes | default(65536) | int)
      tags: [report, artifact, evidence]

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Evidence | Publish the artifacts as job artifacts"
      ansible.builtin.set_stats:
        data:
          fe_evidence:
            "sciencelogic/sl1_audit_governance/10_admin_actions":
              component: "sciencelogic/sl1_audit_governance/10_admin_actions"
              platform: "sciencelogic"
              collected_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
              source_dir: "{{ artifacts_dir }}"
              files: >-
                {{ dict(fe_evidence_read.results | default([])
                        | rejectattr('skipped', 'defined')
                        | map(attribute='item.path') | map('basename') | list
                        | zip(fe_evidence_read.results | default([])
                              | rejectattr('skipped', 'defined')
                              | map(attribute='content') | map('b64decode') | list)) }}
        aggregate: true
        per_host: false
      when:
        - fe_evidence_publish | default(true) | bool
        - fe_evidence_read.results | default([]) | rejectattr('skipped', 'defined') | list | length > 0
      tags: [report, artifact, evidence]

    # ----------------------------- postflight -------------------

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_sciencelogic_sl1_audit_governance_10_admin_actions_preflight_passed | bool
          - fe_sciencelogic_sl1_audit_governance_10_admin_actions_status in ['running', 'succeeded']
        fail_msg: >-
          sciencelogic/sl1_audit_governance/10_admin_actions: postflight validation failed - preflight passed
          ={{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_preflight_passed | default(false) }},
          status={{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_status | default('unknown') }}.
        success_msg: "sciencelogic/sl1_audit_governance/10_admin_actions: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_postflight_passed: true
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_status: "failed"
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_postflight_passed: false
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          sciencelogic/sl1_audit_governance/10_admin_actions: FAILED at '{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_error }}
      tags:
        - always
        - rescue

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          sciencelogic/sl1_audit_governance/10_admin_actions failed at '{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_failed_task }}': {{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_sciencelogic_sl1_audit_governance_10_admin_actions_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "sciencelogic/sl1_audit_governance/10_admin_actions":
              component: "sciencelogic/sl1_audit_governance/10_admin_actions"
              type: "role-tasks"
              platform: "sciencelogic"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_status | default('unknown') }}"
              preflight_passed: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_started_at | default('') }}"
              ended_at: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_ended_at | default('') }}"
              duration_seconds: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_failed_task | default('') }}"
              error: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_sciencelogic_sl1_audit_governance_10_admin_actions_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_sciencelogic_sl1_audit_governance_10_admin_actions_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_sciencelogic_sl1_audit_governance_10_admin_actions_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_sciencelogic_sl1_audit_governance_10_admin_actions_status | default('') == 'succeeded' else ['sciencelogic/sl1_audit_governance/10_admin_actions'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "sciencelogic/sl1_audit_governance/10_admin_actions | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_sciencelogic_sl1_audit_governance_10_admin_actions_stats_published: true
      tags:
        - always
