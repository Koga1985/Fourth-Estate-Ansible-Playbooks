---
- name: Access review report CSV
  when: include_access_reviews | bool
  ansible.builtin.copy:
    dest: "{{ artifacts_dir }}/audit_access_review.csv"
    mode: "0644"
    content: "user,roles,orgs,last_login\n"
