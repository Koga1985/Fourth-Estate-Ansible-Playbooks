---
- name: Collection Groups plan
  ansible.builtin.copy:
    dest: "{{ artifacts_dir }}/collector_groups_plan.json"
    mode: "0644"
    content: "{{ collectors | map(attribute='group') | list | to_nice_json }}"
