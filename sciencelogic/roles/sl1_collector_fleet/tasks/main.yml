---
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir }}"
    state: directory
    mode: "0755"

- import_tasks: 10_provision.yml
- import_tasks: 20_assign_groups.yml
- import_tasks: 30_maintenance.yml
