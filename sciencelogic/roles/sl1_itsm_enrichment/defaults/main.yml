---
sl1:
  url: "https://sl1.example.com"
  token: ""
  verify_ssl: true
artifacts_dir: "/tmp/sl1-artifacts"
dry_run: true

enrichers: []       # [{field:'business_service', lookup:'tag:service'}, {field:'owner', lookup:'cmdb:owner'}]
runbooks: []        # [{pattern:'db-*', url:'https://wiki/runbook-db'}]
status_sync: { enabled: true, state_map: { opened: "New", resolved: "Resolved" } }
