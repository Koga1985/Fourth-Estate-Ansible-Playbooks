---
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir }}"
    state: directory
    mode: "0755"

- import_tasks: tasks/10_accounts.yml
- import_tasks: tasks/20_scopes.yml
- import_tasks: tasks/30_tag_mapping.yml
