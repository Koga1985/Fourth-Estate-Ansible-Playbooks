---
- name: Discovery plan
  ansible.builtin.copy:
    dest: "{{ artifacts_dir }}/inventory_discovery_plan.json"
    mode: "0644"
    content: "{{ {'ranges': ranges | default([]), 'creds': creds | default([]), 'accounts': accounts | default([])} | to_nice_json }}"
