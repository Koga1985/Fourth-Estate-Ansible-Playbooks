---
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir }}"
    state: directory
    mode: "0755"

- import_tasks: 10_powerpacks.yml
- import_tasks: 20_kpi_rules.yml
- import_tasks: 30_cost_labels.yml
