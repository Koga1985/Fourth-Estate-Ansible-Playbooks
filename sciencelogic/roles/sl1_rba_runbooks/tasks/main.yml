---
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir }}"
    state: directory
    mode: "0755"

- import_tasks: tasks/10_actions.yml
- import_tasks: tasks/20_bindings.yml
