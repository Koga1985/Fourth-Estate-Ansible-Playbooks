---
- name: Ensure artifacts dir
  ansible.builtin.file:
    path: "{{ artifacts_dir }}"
    state: directory
    mode: "0755"

- import_tasks: 10_preflight.yml
- import_tasks: 20_backup.yml
- import_tasks: 30_roll_upgrade.yml
