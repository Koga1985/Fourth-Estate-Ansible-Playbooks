---
- name: WMI/SSH creds plan
  ansible.builtin.copy:
    dest: "{{ artifacts_dir }}/server_creds_plan.json"
    mode: "0644"
    content: "{{ {'wmi': wmi_creds, 'ssh': ssh_creds} | to_nice_json }}"
