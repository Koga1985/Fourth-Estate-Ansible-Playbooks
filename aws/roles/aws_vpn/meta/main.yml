---
galaxy_info:
  author: Fourth Estate Platform Team
  description: Production-ready VPN connectivity for AWS GovCloud with FedRAMP High compliance
  company: Fourth Estate
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Amazon Linux
      versions:
        - all
  galaxy_tags:
    - aws
    - vpc
    - vpn
    - networking
    - security
    - fedramp
    - govcloud

dependencies:
  - role: aws_vpc
  - role: aws_cloudwatch_alarms
    when: vpn_enable_monitoring | default(true) | bool

collections:
  - amazon.aws
  - community.aws
