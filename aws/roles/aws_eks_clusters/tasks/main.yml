---
# tasks file for aws_eks_clusters

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['aws/aws_eks_clusters']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_aws_aws_eks_clusters_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "aws/aws_eks_clusters | Initialize validation state"
  ansible.builtin.set_fact:
    fe_aws_aws_eks_clusters_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_aws_aws_eks_clusters_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_aws_aws_eks_clusters_status: "running"
    fe_aws_aws_eks_clusters_preflight_passed: false
    fe_aws_aws_eks_clusters_postflight_passed: false
    fe_aws_aws_eks_clusters_failed_task: ""
    fe_aws_aws_eks_clusters_error: ""
    fe_aws_aws_eks_clusters_stats_published: false
  tags:
    - always

- name: "aws/aws_eks_clusters | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "aws/aws_eks_clusters | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          aws/aws_eks_clusters: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          aws/aws_eks_clusters: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "aws/aws_eks_clusters | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "aws/aws_eks_clusters: required variable '{{ item }}' is not defined."
        success_msg: "aws/aws_eks_clusters: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_aws_aws_eks_clusters_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "aws/aws_eks_clusters | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_aws_aws_eks_clusters_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Create EKS cluster
      community.aws.eks_cluster:
        name: "{{ eks_cluster_name }}"
        version: "{{ eks_version }}"
        role_arn: "{{ eks_role_arn }}"
        subnets: "{{ eks_subnet_ids }}"
        security_groups: "{{ eks_security_group_ids }}"
        region: "{{ eks_region }}"
        state: "{{ eks_state }}"
        tags: "{{ eks_tags }}"
      register: eks_result

    - name: Display EKS cluster information
      ansible.builtin.debug:
        msg:
          - "EKS Cluster: {{ eks_cluster_name }}"
          - "Version: {{ eks_version }}"
          - "Endpoint: {{ eks_result.cluster.endpoint | default('N/A') }}"

    # ----------------------------- postflight -------------------

    - name: "aws/aws_eks_clusters | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_aws_aws_eks_clusters_preflight_passed | bool
          - fe_aws_aws_eks_clusters_status in ['running', 'succeeded']
        fail_msg: >-
          aws/aws_eks_clusters: postflight validation failed - preflight passed
          ={{ fe_aws_aws_eks_clusters_preflight_passed | default(false) }},
          status={{ fe_aws_aws_eks_clusters_status | default('unknown') }}.
        success_msg: "aws/aws_eks_clusters: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "aws/aws_eks_clusters | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_aws_aws_eks_clusters_postflight_passed: true
        fe_aws_aws_eks_clusters_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "aws/aws_eks_clusters | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_aws_aws_eks_clusters_status: "failed"
        fe_aws_aws_eks_clusters_postflight_passed: false
        fe_aws_aws_eks_clusters_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_aws_aws_eks_clusters_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "aws/aws_eks_clusters | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          aws/aws_eks_clusters: FAILED at '{{ fe_aws_aws_eks_clusters_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_aws_aws_eks_clusters_error }}
      tags:
        - always
        - rescue

    - name: "aws/aws_eks_clusters | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          aws/aws_eks_clusters failed at '{{ fe_aws_aws_eks_clusters_failed_task }}': {{ fe_aws_aws_eks_clusters_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "aws/aws_eks_clusters | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_aws_aws_eks_clusters_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_aws_aws_eks_clusters_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_aws_aws_eks_clusters_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "aws/aws_eks_clusters | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "aws/aws_eks_clusters":
              component: "aws/aws_eks_clusters"
              type: "role"
              platform: "aws"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_aws_aws_eks_clusters_status | default('unknown') }}"
              preflight_passed: "{{ fe_aws_aws_eks_clusters_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_aws_aws_eks_clusters_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_aws_aws_eks_clusters_started_at | default('') }}"
              ended_at: "{{ fe_aws_aws_eks_clusters_ended_at | default('') }}"
              duration_seconds: "{{ fe_aws_aws_eks_clusters_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_aws_aws_eks_clusters_failed_task | default('') }}"
              error: "{{ fe_aws_aws_eks_clusters_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_aws_aws_eks_clusters_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_aws_aws_eks_clusters_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_aws_aws_eks_clusters_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_aws_aws_eks_clusters_status | default('') == 'succeeded' else ['aws/aws_eks_clusters'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "aws/aws_eks_clusters | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_aws_aws_eks_clusters_stats_published: true
      tags:
        - always
