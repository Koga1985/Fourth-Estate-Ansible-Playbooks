---
galaxy_info:
  author: Fourth Estate Platform Team
  description: Production-ready EBS volume management for AWS GovCloud with FedRAMP High compliance
  company: Fourth Estate
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Amazon Linux
      versions:
        - all
  galaxy_tags:
    - aws
    - ec2
    - ebs
    - storage
    - volumes
    - fedramp
    - govcloud

dependencies:
  - role: aws_kms
  - role: aws_cloudwatch_alarms
    when: ebs_enable_cloudwatch_alarms | default(true) | bool

collections:
  - amazon.aws
  - community.aws
