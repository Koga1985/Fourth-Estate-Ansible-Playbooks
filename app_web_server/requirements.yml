---
# Application & Web Server SRG Ansible Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  # htpasswd, ini_file and other utility modules used by the SRG roles
  - name: community.general
    version: ">=7.0.0"
