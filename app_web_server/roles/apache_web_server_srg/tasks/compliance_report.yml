---
# Apache Web Server SRG compliance artifact (JSON).

# ---------------------------------------------------------------------------
# Fourth Estate preflight / postflight validation harness
#
# The task file body below is wrapped in an Ansible block/rescue/always structure:
#
#   block   preflight validation runs first, then the original task file body, then
#           postflight validation. Any failure transfers control to rescue.
#   rescue  records which task failed and why, then re-raises so a failure is
#           never silently swallowed. Set fe_validation_continue_on_error=true
#           to record the failure and keep going instead.
#   always  computes the run duration and publishes the outcome through
#           ansible.builtin.set_stats, so a wrapping playbook, AWX/AAP or CI can
#           consume the result without scraping log output.
#
# Published stats (see docs/VALIDATION_AND_STATS.md):
#   fe_validation_results['app_web_server/apache_web_server_srg/compliance_report']  per-component record
#   fe_validation_executions / _succeeded / _failed / _duration_seconds
#   fe_validation_failed_components
#
# Add component-specific preflight checks without editing this file by setting
#   fe_app_web_server_apache_web_server_srg_compliance_report_required_vars: [var_one, var_two]
# ---------------------------------------------------------------------------

- name: "app_web_server/apache_web_server_srg/compliance_report | Initialize validation state"
  ansible.builtin.set_fact:
    fe_app_web_server_apache_web_server_srg_compliance_report_start_epoch: "{{ now(fmt='%s') | int }}"
    fe_app_web_server_apache_web_server_srg_compliance_report_started_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
    fe_app_web_server_apache_web_server_srg_compliance_report_status: "running"
    fe_app_web_server_apache_web_server_srg_compliance_report_preflight_passed: false
    fe_app_web_server_apache_web_server_srg_compliance_report_postflight_passed: false
    fe_app_web_server_apache_web_server_srg_compliance_report_failed_task: ""
    fe_app_web_server_apache_web_server_srg_compliance_report_error: ""
    fe_app_web_server_apache_web_server_srg_compliance_report_stats_published: false
  tags:
    - always

- name: "app_web_server/apache_web_server_srg/compliance_report | Guarded execution with preflight and postflight validation"
  block:

    # ----------------------------- preflight --------------------

    - name: "app_web_server/apache_web_server_srg/compliance_report | Preflight | Verify the control node meets the minimum ansible-core version"
      ansible.builtin.assert:
        that:
          - ansible_version.full is version(fe_validation_min_ansible_version | default('2.12'), '>=')
        fail_msg: >-
          app_web_server/apache_web_server_srg/compliance_report: requires ansible-core
          {{ fe_validation_min_ansible_version | default('2.12') }} or newer,
          found {{ ansible_version.full }}.
        success_msg: >-
          app_web_server/apache_web_server_srg/compliance_report: ansible-core {{ ansible_version.full }} satisfies the minimum.
        quiet: true
      tags:
        - always
        - preflight
        - validation

    - name: "app_web_server/apache_web_server_srg/compliance_report | Preflight | Verify every declared required variable is defined"
      ansible.builtin.assert:
        that:
          - vars[item] is defined
        fail_msg: "app_web_server/apache_web_server_srg/compliance_report: required variable '{{ item }}' is not defined."
        success_msg: "app_web_server/apache_web_server_srg/compliance_report: required variable '{{ item }}' is defined."
        quiet: true
      loop: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_required_vars | default([]) }}"
      loop_control:
        label: "{{ item }}"
      tags:
        - always
        - preflight
        - validation

    - name: "app_web_server/apache_web_server_srg/compliance_report | Preflight | Record that preflight validation passed"
      ansible.builtin.set_fact:
        fe_app_web_server_apache_web_server_srg_compliance_report_preflight_passed: true
      tags:
        - always
        - preflight
        - validation

    # ----------------------------- execute ----------------------

    - name: Assemble result map
      ansible.builtin.set_fact:
        apache_srg_results:
          meta:
            host: "{{ inventory_hostname }}"
            timestamp: "{{ ansible_date_time.iso8601 | default('') }}"
            benchmark: "{{ stig_benchmark }}"
            version: "{{ stig_version }}"
            mode: "{{ 'enforce' if apply_changes | bool else 'dry-run' }}"
            compliance_frameworks: "{{ compliance_frameworks }}"
          controls:
            - { id: "SRG-APP-000516-WSR-000174", title: "Suppress version disclosure", would_change: "{{ apache_dropin.changed | default(false) }}" }
            - { id: "SRG-APP-000439-WSR-000156", title: "TLS protocol/cipher hardening", would_change: "{{ apache_dropin.changed | default(false) }}" }
            - { id: "SRG-APP-000211-WSR-000031", title: "Directory browsing disabled", would_change: "{{ (apache_loadmodule.changed | default(false)) or (apache_modules_marker.changed | default(false)) }}" }
            - { id: "SRG-APP-000141-WSR-000081", title: "HTTP TRACE disabled", would_change: "{{ apache_dropin.changed | default(false) }}" }
      tags: [report]

    - name: Write JSON compliance artifact
      ansible.builtin.copy:
        dest: "{{ artifacts_dir }}/{{ inventory_hostname }}_apache_web_srg.json"
        mode: "0640"
        content: "{{ apache_srg_results | to_nice_json }}"
      delegate_to: localhost
      tags: [report]

    - name: Summarize
      ansible.builtin.debug:
        msg: >-
          {{ inventory_hostname }}: {{ apache_srg_results.controls | selectattr('would_change') | list | length }}
          of {{ apache_srg_results.controls | length }} controls
          {{ 'pending (dry-run)' if not (apply_changes | bool) else 'remediated' }}.
      tags: [report]

    # ----------------------------- postflight -------------------

    - name: "app_web_server/apache_web_server_srg/compliance_report | Postflight | Verify the guarded body ran after a passing preflight"
      ansible.builtin.assert:
        that:
          - fe_app_web_server_apache_web_server_srg_compliance_report_preflight_passed | bool
          - fe_app_web_server_apache_web_server_srg_compliance_report_status in ['running', 'succeeded']
        fail_msg: >-
          app_web_server/apache_web_server_srg/compliance_report: postflight validation failed - preflight passed
          ={{ fe_app_web_server_apache_web_server_srg_compliance_report_preflight_passed | default(false) }},
          status={{ fe_app_web_server_apache_web_server_srg_compliance_report_status | default('unknown') }}.
        success_msg: "app_web_server/apache_web_server_srg/compliance_report: postflight validation passed."
        quiet: true
      tags:
        - always
        - postflight
        - validation

    - name: "app_web_server/apache_web_server_srg/compliance_report | Postflight | Record the successful outcome"
      ansible.builtin.set_fact:
        fe_app_web_server_apache_web_server_srg_compliance_report_postflight_passed: true
        fe_app_web_server_apache_web_server_srg_compliance_report_status: "succeeded"
      tags:
        - always
        - postflight
        - validation

  rescue:

    - name: "app_web_server/apache_web_server_srg/compliance_report | Rescue | Capture the failing task and error detail"
      ansible.builtin.set_fact:
        fe_app_web_server_apache_web_server_srg_compliance_report_status: "failed"
        fe_app_web_server_apache_web_server_srg_compliance_report_postflight_passed: false
        fe_app_web_server_apache_web_server_srg_compliance_report_failed_task: "{{ ansible_failed_task.name | default('unknown task') }}"
        fe_app_web_server_apache_web_server_srg_compliance_report_error: "{{ ansible_failed_result.msg | default('no error detail reported') }}"
      tags:
        - always
        - rescue

    - name: "app_web_server/apache_web_server_srg/compliance_report | Rescue | Report the failure"
      ansible.builtin.debug:
        msg: >-
          app_web_server/apache_web_server_srg/compliance_report: FAILED at '{{ fe_app_web_server_apache_web_server_srg_compliance_report_failed_task }}' on
          {{ inventory_hostname }} - {{ fe_app_web_server_apache_web_server_srg_compliance_report_error }}
      tags:
        - always
        - rescue

    - name: "app_web_server/apache_web_server_srg/compliance_report | Rescue | Re-raise the failure unless the caller opted to continue"
      ansible.builtin.fail:
        msg: >-
          app_web_server/apache_web_server_srg/compliance_report failed at '{{ fe_app_web_server_apache_web_server_srg_compliance_report_failed_task }}': {{ fe_app_web_server_apache_web_server_srg_compliance_report_error }}
      when: not (fe_validation_continue_on_error | default(false) | bool)
      tags:
        - always
        - rescue

  always:

    - name: "app_web_server/apache_web_server_srg/compliance_report | Always | Compute the execution duration"
      ansible.builtin.set_fact:
        fe_app_web_server_apache_web_server_srg_compliance_report_ended_at: "{{ now(utc=true, fmt='%Y-%m-%dT%H:%M:%SZ') }}"
        fe_app_web_server_apache_web_server_srg_compliance_report_duration_seconds: "{{ (now(fmt='%s') | int) - (fe_app_web_server_apache_web_server_srg_compliance_report_start_epoch | default(0) | int) }}"
      tags:
        - always

    - name: "app_web_server/apache_web_server_srg/compliance_report | Always | Publish the validation result with set_stats"
      ansible.builtin.set_stats:
        data:
          fe_validation_results:
            "app_web_server/apache_web_server_srg/compliance_report":
              component: "app_web_server/apache_web_server_srg/compliance_report"
              type: "role-tasks"
              platform: "app_web_server"
              host: "{{ inventory_hostname }}"
              status: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_status | default('unknown') }}"
              preflight_passed: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_preflight_passed | default(false) | bool }}"
              postflight_passed: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_postflight_passed | default(false) | bool }}"
              started_at: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_started_at | default('') }}"
              ended_at: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_ended_at | default('') }}"
              duration_seconds: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_duration_seconds | default(0) | int }}"
              failed_task: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_failed_task | default('') }}"
              error: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_error | default('') }}"
              check_mode: "{{ ansible_check_mode | default(false) | bool }}"
          fe_validation_executions: 1
          fe_validation_succeeded: "{{ 1 if fe_app_web_server_apache_web_server_srg_compliance_report_status | default('') == 'succeeded' else 0 }}"
          fe_validation_failed: "{{ 0 if fe_app_web_server_apache_web_server_srg_compliance_report_status | default('') == 'succeeded' else 1 }}"
          fe_validation_duration_seconds: "{{ fe_app_web_server_apache_web_server_srg_compliance_report_duration_seconds | default(0) | int }}"
          fe_validation_failed_components: >-
            {{ [] if fe_app_web_server_apache_web_server_srg_compliance_report_status | default('') == 'succeeded' else ['app_web_server/apache_web_server_srg/compliance_report'] }}
        aggregate: true
        per_host: false
      tags:
        - always

    - name: "app_web_server/apache_web_server_srg/compliance_report | Always | Mark the validation statistics as published"
      ansible.builtin.set_fact:
        fe_app_web_server_apache_web_server_srg_compliance_report_stats_published: true
      tags:
        - always
