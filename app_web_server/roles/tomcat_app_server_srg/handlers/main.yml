---
# handlers file for tomcat_app_server_srg

- name: restart tomcat
  ansible.builtin.systemd:
    name: "{{ tomcat_service_name }}"
    state: restarted
  when: apply_changes | bool
  listen: restart tomcat
