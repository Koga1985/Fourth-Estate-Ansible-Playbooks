---
# NetApp Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: netapp.ontap
    version: ">=22.8.0"

  - name: community.general
    version: ">=8.0.0"

# Python dependencies:
# pip install netapp-lib
