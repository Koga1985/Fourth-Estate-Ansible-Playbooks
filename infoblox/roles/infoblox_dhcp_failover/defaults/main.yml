# Connection
nios_host: ""
nios_username: ""
nios_password: ""
nios_validate_certs: true  # secure default; set false only for bootstrap against self-signed certs
nios_wapi_version: "v2.12"

# Scope / behavior
network_view: "default"
artifact_dir: "/tmp/infoblox-dhcp-failover"

# Input: list of peering definitions (see README for schema)
dhcp_failover_pairs: []
