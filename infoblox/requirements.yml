---
# Infoblox Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: infoblox.nios_modules
    version: ">=1.5.0"

  - name: community.general
    version: ">=8.0.0"

# Python dependencies:
# pip install infoblox-client
